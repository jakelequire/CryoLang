namespace Sandbox;

import <io/stdio>;


function foo() -> void {
    

    return;
}

function main() -> int {
    printf("Hello, world!\n");
    const p: void * = cryo_alloc(128);
    printf("Allocated 128 bytes at address: %p\n", p);

    const n: int = 10;
    const fib_n: int = fibonacci(n);
    printf("Fibonacci(%d) = %d\n", n, fib_n);
    
    return 0;
}