namespace Sandbox;

import <io/stdio>;

function main() -> int {
    printf("Hello, world!\n");

    const p: void * = cryo_alloc(1024);
    printf("Allocated 1024 bytes at address %p\n", p);

    cryo_free(p);
    printf("Freed allocated memory\n");

    return 0;
}
