namespace Main;

import <io/stdio>;

function main() -> int {
    printf("Hello, Cryo World!\n");

    const s: string = "Sample string";
    printf("This is a string: %s", s);

    const len: int = s.length();
    printf("Length of the string: %d\n", len);

    return 0;
}