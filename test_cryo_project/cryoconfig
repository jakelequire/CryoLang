# Cryo Project Configuration
# This file defines the build configuration for your Cryo project

project_name = "hello_world"
output_dir = "build"
target_type = "executable"

# Compiler options
[compiler]
debug = false
optimize = true
emit_llvm = false

# Dependencies (future feature)
[dependencies]
# Add dependencies here
