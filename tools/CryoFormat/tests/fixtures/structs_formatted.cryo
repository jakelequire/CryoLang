// Test fixture for struct declarations
type struct Point {
    x: int,
    y: int
}

type struct Person {
    name: string,
    age: int,
    email: string
}

type struct Complex {
    real: float,
    imaginary: float,
    metadata: string
}