///! ast_query — read-only queries over a compiled AST.
///!
///! Used by the post-compile feature handlers (hover, definition,
///! completion) to translate a cursor position into a meaningful
///! AST node + symbol + type.

namespace AstQuery;

public module ast_query::position_finder;
public module ast_query::scope_index;
