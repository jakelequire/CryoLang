///! Byte-offset ↔ (line, character) conversion.
///!
///! Phase A stub — populated in Phase B alongside `docs` and the
///! diagnostic converter (which needs to translate the compiler's
///! 1-based line/col SourceSpan into the LSP's 0-based UTF-16 form).

namespace Lsp::Server::LineIndex;

import std::collections::str;
