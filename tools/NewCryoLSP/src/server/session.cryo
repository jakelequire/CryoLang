///! Per-document compiler session — Phase B.
///!
///! Each open document keeps its own `CompilationContext` alive across
///! requests so hover/definition handlers can walk the AST + types
///! without re-parsing.  Filled in once Phase A is verified.

namespace Lsp::Server::Session;

import std::collections::str;
