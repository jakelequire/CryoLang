///! protocol — LSP / JSON-RPC framing, message types, and conversions.
///!
///! Sub-modules:
///!   • `framing`      — Content-Length read/write
///!   • `jsonrpc`      — Request/Response/Notification + parse/encode
///!   • `lsp_types`    — Position, Range, Location, Hover, CompletionItem...
///!   • `capabilities` — `build_initialize_response`
///!   • `conv`         — span↔range, severity-string↔int, file path↔URI

namespace Protocol;

public module protocol::framing;
public module protocol::jsonrpc;
public module protocol::lsp_types;
public module protocol::capabilities;
public module protocol::conv;
