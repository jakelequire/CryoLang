///! Conversions between compiler-internal types and LSP wire types.
///!
///! Phase A stub — populated in Phase B when diagnostic publishing,
///! hover, and definition land.  Centralising the conversions here
///! keeps each handler focused on dispatch rather than wire format.

namespace Lsp::Protocol::Conv;

import std::collections::str;
