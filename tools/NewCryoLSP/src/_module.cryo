///! Top-level aggregator for the LSP source tree.  Imports each
///! sibling namespace so the loader's transitive walk reaches every
///! module without an explicit `[compiler] include_paths` list.

namespace Lsp;

import Lsp::Protocol;
import Lsp::Server;
import Lsp::Handlers;
