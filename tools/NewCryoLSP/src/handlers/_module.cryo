///! handlers — one file per LSP capability.
///!
///! Lifecycle (initialize/shutdown/exit) and text-sync (didOpen/Change/
///! Close/Save) live here, alongside the four post-compile capabilities
///! (hover, definition, completion) plus the diagnostics push helper.

namespace Handlers;

public module handlers::lifecycle;
public module handlers::text_sync;
public module handlers::diagnostics;
public module handlers::hover;
public module handlers::definition;
public module handlers::completion;
