// Fixture for hover assertions: typed locals + a function call site.

namespace Fixtures::HoverSubject;

function double(n: i32) -> i32 {
    return n * 2;
}

function main() -> int {
    const local_int:    i32     = 42;
    const local_uint:   u64     = 100;
    const local_float:  f64     = 3.14;
    const result: i32 = double(local_int);
    return result as int;
}
