// Deliberate name-resolution error — refers to undeclared identifier.

namespace Fixtures::UndefinedName;

function main() -> int {
    return some_undeclared_thing;  // Expected: name resolution error.
}
