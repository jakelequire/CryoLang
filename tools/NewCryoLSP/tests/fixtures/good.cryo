// A clean compile — no diagnostics expected.

namespace Fixtures::Good;

function add(a: i32, b: i32) -> i32 {
    return a + b;
}

function main() -> int {
    const x: i32 = add(2, 3);
    return x as int;
}
