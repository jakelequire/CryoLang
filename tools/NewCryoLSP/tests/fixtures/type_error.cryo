// Deliberate type error — assigning a string to an i32 binding.

namespace Fixtures::TypeError;

function main() -> int {
    const x: i32 = "hello";   // Expected: error — "hello" is not i32.
    return 0;
}
