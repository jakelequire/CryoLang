namespace Main;

import CLI;

function main(argc: int, argv: string*) -> int {
    const runner: CLI::Runner = CLI::Runner::new("cryoc", "0.1.0")
                                .init_commands();
    runner.run(argc, argv);

    return 0;
}
