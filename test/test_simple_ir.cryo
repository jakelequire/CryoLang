namespace Main;

function add(a: int, b: int) -> int {
    return a + b;
}

function main() -> int {
    const x: int = 10;
    const y: int = 20;
    const result: int = add(x, y);
    return result;
}
