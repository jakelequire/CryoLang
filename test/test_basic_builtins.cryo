namespace Main;

// Simple test with only basic built-in functions
function test_basic_builtins() -> void {
    const message: string = "Hello World";
    
    // Test I/O functions
    print(message);
    println("Testing basic functions");
    
    // Test math functions
    const value: float = sqrt(16.0);
    const power: float = pow(2.0, 3.0);
    
    return;
}

function main(argc: int, argv: string[]) -> int {
    test_basic_builtins();
    return 0;
}