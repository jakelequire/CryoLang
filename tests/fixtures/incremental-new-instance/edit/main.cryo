import std::thread;

type struct JobA { n: i32; }
type struct JobB { m: i64; }

function run_a(j: JobA) -> void { }
function run_b(j: JobB) -> void { }

function main() -> i32 {
    {
        mut scope: thread::Scope = thread::Scope::new();
        scope.spawn::<JobA>(JobA { n: 1 }, run_a);
        scope.spawn::<JobB>(JobB { m: 2 }, run_b);
    }
    return 4;
}
