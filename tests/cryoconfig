# Cryo Test Suite - repo-level e2e tests.
#
# This project exercises the language, the compiler, and the standard
# library by way of `cryo test`.  Test files live in `tests/` (a sibling
# of `src/`); each file's namespace declaration carries `![config(testing)]`,
# and each test function carries `![test]`.  See docs/testing.md for the
# full surface.
#
# `src/main.cryo` is a placeholder that `cryo test` suppresses - the
# compiler synthesises a test-runner main that drives every discovered
# `![test]` function.  Running `cryo run` from this project would invoke
# the placeholder main; running `cryo test` is the canonical entry point.

[project]
project_name = "cryo-tests"
output_dir   = "build"
target_type  = "executable"
entry_point  = "src/main.cryo"

[compiler]
debug     = false
# Build the test suite optimized (O2) - the production default.  The old
# blocker for an O0 matrix (the LLVM `va_arg` instruction's broken -O0
# SysV lowering, which segfaulted VaArgs `.next<T>()`) is gone: the
# compiler now emits the inline SysV register-save-area expansion at
# every -O level (IntrinsicEmitter::emit_va_arg_sysv), so an O0
# (`optimize = O0`) test matrix is now viable as a follow-up.
optimize  = O2
emit_llvm = true

[link]
# `ssl` + `crypto`: net::tls (net_tls.cryo) binds system OpenSSL.
# Anything that pulls in std::net::tls must list these under [link] system;
# the stdlib archive only declares the symbols.
system = ["ssl", "crypto"]

# C-side ABI test helpers: a local static archive built by the top-level
# Makefile (see helpers/abi_helpers.c).  Linked by path so the linker
# resolves the `abi_*` symbols the test objects reference.  The archive
# name is per-OS (the Makefile builds `-unix` or `-windows` to match the
# host): a Windows and a WSL/Linux `make test` share this working tree,
# and one OS-agnostic archive path would let a stale PE archive satisfy
# the Linux link (and vice versa).
[link.unix]
static = ["helpers/libabihelpers-unix.a"]

[link.windows]
static = ["helpers/libabihelpers-windows.a"]

[dependencies]

# Repo-level test suite opts into the grouped, color-on-TTY layout so the
# 800+ tests are scannable when run interactively. End-user projects with
# no `[test]` section get the cargo-style `plain` default. Override per-run
# with `cryo test --format=plain` or `CRYO_TEST_FORMAT=...`.
[test]
format = "pretty"
color  = "auto"
