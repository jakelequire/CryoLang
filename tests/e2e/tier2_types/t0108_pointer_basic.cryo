// @test: pointer_basic
// @expect: compile_success
// @mode: raw
// @stdout: 42
// @tags: pointers

namespace PtrBasic;

function main() -> int {
    mut x: int = 42;
    const p: int* = &x;
    printf("%d\n", *p);
    return 0;
}
