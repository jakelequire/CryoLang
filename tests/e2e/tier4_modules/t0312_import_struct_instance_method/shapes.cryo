namespace Shapes;

type struct Rect {
    w: int;
    h: int;

    static new(w: int, h: int) -> Rect {
        return Rect { w: w, h: h };
    }

    area(&this) -> int {
        return this.w * this.h;
    }
}
