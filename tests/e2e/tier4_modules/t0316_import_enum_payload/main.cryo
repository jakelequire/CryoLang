// @test: import_enum_payload
// @expect: compile_success
// @mode: project
// @stdout: 5
// @tags: modules, project, import, enums, payload

namespace Main;

import Shapes::Shape;

function main() -> int {
    const s: Shape = Shape::Circle(5);
    match (s) {
        Shape::Circle(r) => {
            printf("%d\n", r);
        },
        Shape::Square(side) => {
            printf("%d\n", side);
        },
    }
    return 0;
}
