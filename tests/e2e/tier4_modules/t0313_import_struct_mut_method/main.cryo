// @test: import_struct_mut_method
// @expect: compile_success
// @mode: project
// @stdout: 3
// @tags: modules, project, import, structs, mutation

namespace Main;

import Counter;

function main() -> int {
    mut c: Counter::Ctr = Counter::Ctr::new();
    c.increment();
    c.increment();
    c.increment();
    printf("%d\n", c.get());
    return 0;
}
