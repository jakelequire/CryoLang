// @test: import_nonexistent_type
// @expect: compile_fail
// @mode: project
// @tags: modules, project, import, negative

namespace Main;

import Stuff::Bar;

function main() -> int {
    const b: Bar = Bar::new();
    return 0;
}
