namespace Store;

import Utils;

type struct Registry {
    data: Bucket<int>;

    static new() -> Registry {
        return Registry {
            data: Bucket<int>::new(),
        };
    }

    put(val: int) -> void {
        this.data.add(val);
    }

    total() -> int {
        return this.data.size();
    }
}
