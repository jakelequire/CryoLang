// @test: cross_module_struct_generic_return
// @expect: compile_success
// @mode: project
// @stdout: name: test
// @stdout: ready: 0
// @tags: modules, project, generics, cross_module, struct_method, two_param_generic, regression
// @description: Struct method returning cross-module generic enum type. Mirrors cryoc bug: Method 'load' has unresolved return type: <error: unresolved generic: Result<boolean,string>>

namespace Main;

import Handler;

function main() -> int {
    const h: Handler::Handler = Handler::Handler::new("test");
    printf("name: %s\n", h.name());
    printf("ready: %d\n", h.is_ready());
    return 0;
}
