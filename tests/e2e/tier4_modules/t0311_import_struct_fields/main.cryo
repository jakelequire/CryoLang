// @test: import_struct_fields
// @expect: compile_success
// @mode: project
// @stdout: 42
// @tags: modules, project, import, structs

namespace Main;

import Geo;

function main() -> int {
    const p: Geo::Point = Geo::Point::new(10, 32);
    printf("%d\n", p.x + p.y);
    return 0;
}
