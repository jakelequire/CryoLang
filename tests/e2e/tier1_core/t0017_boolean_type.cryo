// @test: boolean_type
// @expect: compile_success
// @mode: raw
// @stdout: 1
// @stdout: 0
// @tags: types, boolean

namespace BooleanType;

function main() -> int {
    const t: boolean = true;
    const f: boolean = false;
    printf("%d\n", t);
    printf("%d\n", f);
    return 0;
}
