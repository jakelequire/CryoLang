// @test: generic_function_basic
// @expect: compile_success
// @mode: raw
// @stdout: 42
// @tags: generics, functions

namespace GenFuncBasic;

function identity<T>(x: T) -> T {
    return x;
}

function main() -> int {
    const a: int = identity<int>(42);
    printf("%d\n", a);
    return 0;
}
