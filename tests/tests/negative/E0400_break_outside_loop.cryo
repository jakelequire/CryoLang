// E0400: `break` with no enclosing loop. (Compiler gap: not yet diagnosed,
// so this test currently fails — by design, an honest red.)
![config(testing)]
![config(negative, E0400)]
namespace CryoTests::Negative::BreakOutsideLoop;
function main() -> int { break; return 0; }
