// W0013: a printf conversion whose argument type does not match.
![config(testing)]
![config(negative, W0013)]
namespace CryoTests::Negative::W0013FormatStringMismatch;

function main() -> int {
    printf("%d\n", "not a number");   // %d expects an int
    return 0;
}
