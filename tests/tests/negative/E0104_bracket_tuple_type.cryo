// E0104: the pre-1.0 bracket tuple spelling `[T, U]` was removed in favour of
// the parenthesized form `(T, U)`. A leading `[` in type position is now a
// hard error pointing at the migration (array suffixes `T[]`/`T[N]` are
// postfix and remain valid). See parser/expr_parser.cryo parse_type_annotation.
![config(testing)]
![config(negative, E0104)]
namespace CryoTests::Negative::BracketTupleType;

function cf_f() -> [i32, i32] { return (1, 2); }
//~^ ERROR[E0104] bracket tuple types '[T, U]' are no longer supported; use '(T, U)'
//~^^ ERROR[E0100] expected ';' or '{', found '['

function main() -> int { return 0; }
