// W0001: a local binding that is never referenced.
![config(testing)]
![config(negative, W0001)]
namespace CryoTests::Negative::W0001UnusedVariable;

function main() -> int {
    const unused_local: int = 5;   // never read
    return 0;
}
