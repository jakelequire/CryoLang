/// Beside the declaring module, not under it: `protected` does not reach here.
namespace VisibilityModulePrivate::Sibling;

import VisibilityModulePrivate::Vault;
import VisibilityModulePrivate::Vault::{ Account };

public function sibling_reads(a: Account*) -> i32 { return a.for_sibling; }
