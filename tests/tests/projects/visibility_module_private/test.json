{
  "outcome": "compile_fail",
  "expect": {
    "diagnostic": "E0353",
    "output_contains": [
      "field `secret` of",
      "field `sealed` of",
      "field `stamp` of",
      "field `seal` of",
      "field `hidden` of",
      "field `for_sibling` of",
      "method `tuck` of"
    ],
    "output_excludes": [
      "field `near` of",
      "field `for_child` of",
      "field `own` of",
      "field `tag` of"
    ]
  }
}
