// A C function declared and defined nowhere: the program compiles, and the
// link fails on the undefined symbol.
extern "C" {
    function cryo_link_report_missing_fn(v: i32) -> i32;
}

function main() -> i32 {
    return cryo_link_report_missing_fn(3);
}
