/// Reads the PLAIN twin of each leaf through its qualified path.  The
/// attributed module is imported first so its registrations come first.
namespace GlobalLeaf::Main;

import GlobalLeaf::Attributed;
import GlobalLeaf::Plain;

function main() -> int {
    return Plain::counter + Plain::probe;
}
