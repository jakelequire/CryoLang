namespace QualifiedTypeSelectsModule::B;

// Same leaf as A's, a different field, and a different width.
public type struct Widget {
    by: i64;

    static make(v: i64) -> Widget { return Widget { by: v }; }
}
