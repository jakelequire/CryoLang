/// Discovery is import-driven, so the rejected module has to be imported or
/// it is never parsed and this project asserts nothing.
namespace ReexportGlob::Main;

import ReexportGlob::Facade;

function main() -> int {
    return 0;
}
