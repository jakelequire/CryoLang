namespace ModuleTypeImportCollision::Gpu::Vertex;

// Named unlike anything it declares; the collision it takes part in is
// completed by its importer.
public type struct Layout { stride: u32; }

public function count() -> i32 { return 1; }
