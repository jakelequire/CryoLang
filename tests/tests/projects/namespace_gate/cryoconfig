# namespace_gate - the §3.3 reachability gate (E0240), pinned.
#
# Read src/orphan.cryo first. `cryo build` must FAIL here with E0240 naming
# `Crate` and the module that declares it, and must NOT reject the two
# CONTROLS in src/porter.cryo, which reach the same type legally.
#
# This is the compile_fail half of the flip described in
# tests/tests/projects/resolution_tripwire: that project pinned "a bare name
# resolves with nothing in scope" as a PASSING test because the compiler was
# wrong. The gate closed the ANNOTATION door, so that arm moved here as an
# error. The tripwire keeps its static-template arm, which is still open.

[project]
project_name = "namespace_gate"
output_dir   = "build"
target_type  = "executable"
source_dir   = "src"
entry_point  = "src/main.cryo"

[compiler]

[dependencies]
