# resolution_unreachable_module - E0240 for a module-suffix scope whose module
# the writing file cannot reach.
#
# Read src/main.cryo first. `Text` names a real, compiled module that this file
# does not import; binding to it anyway is the defect
# tests/tests/projects/resolution_module_suffix pins from the other side.

[project]
project_name = "resolution_unreachable_module"
output_dir   = "build"
target_type  = "executable"
source_dir   = "src"
entry_point  = "src/main.cryo"

[compiler]

[dependencies]
