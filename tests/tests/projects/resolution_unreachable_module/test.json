{
  "outcome": "compile_fail",
  "expect": {
    "diagnostic": "E0240",
    "output_contains": [
      "ResolutionUnreachableModule::Alpha::Text"
    ],
    "output_excludes": [
      "`Only` is not reachable"
    ]
  }
}
