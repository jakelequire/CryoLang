namespace ExternSymbolCrossModule::CBind;

/// A C function this module declares and never calls: its symbol is held
/// only by the modules that call it, which must find it through the index.
extern "C" {
    function atoi(s: u8*) -> i32;
}
