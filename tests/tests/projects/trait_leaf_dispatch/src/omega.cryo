/// Shares its leaf with `TraitLeafDispatch::Alpha::Render` and is otherwise
/// unrelated to it. The plural leaf is the whole variable.
namespace TraitLeafDispatch::Omega;

type trait Render { render(&this) -> int; }
