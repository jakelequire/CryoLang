namespace static_call_hint_leaf_collision::alpha;

public type struct Beacon {
    v: i32;
}

implement Beacon {
    public static make(v: u8) -> Beacon { return Beacon { v: v as i32 }; }
    public signal(&this) -> i32 { return this.v; }
}
