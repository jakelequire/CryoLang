type struct __Closure_0 {
    v: i32;
}

implement struct __Closure_0 {
    __call__(&this, x: i32) -> i32 { return 1000; }
}

function main() -> i32 {
    const bias: i32 = 10;
    const add_bias = (x: i32) -> i32 { return x + bias; };
    return add_bias(32);
}
