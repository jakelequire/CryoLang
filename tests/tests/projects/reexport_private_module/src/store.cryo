namespace ReexportPrivateModule::Store;

private type struct Hidden {
    v: i32;

    get(&this) -> i32 { return this.v; }
}

type struct Shown {
    v: i32;

    get(&this) -> i32 { return this.v; }
}
