{
  "outcome": "compile_fail",
  "expect": {
    "diagnostic": "E0240",
    "output_contains": [
      "Hidden",
      "ReexportPrivateModule::Store",
      "so no import makes it reachable"
    ],
    "output_excludes": [
      "add `import"
    ]
  }
}
