/// Shares its leaf with `WhereBoundLeafCollision::Alpha::Render` and is
/// otherwise unrelated to it. Making the leaf plural is the whole variable.
namespace WhereBoundLeafCollision::Omega;

type trait Render { render(&this) -> int; }
