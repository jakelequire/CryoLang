# ffi_cpp_link - the C++ direct-mangled-binding exemplar (Compiler::Bindgen
# Strategy A).  The `CppLink` bindings (bindings/CppLink.cryo, added via
# `source_paths`) pin a C++ namespace function and class methods to their
# Itanium-mangled symbols with `![link("...")]`; the project's `![test]`s (under
# tests/) call them from another module and assert.  Linking resolves those
# symbols against `tests/helpers/libcpplinkhelper-unix.a`, which the Makefile
# builds with the host C++ compiler before the project suite runs (`-lstdc++`
# pulls in the C++ runtime).  The archive carries the per-OS `-unix` suffix
# (helper artifacts are never shared across host OSes - see the Makefile) and
# is listed under `[link.unix]`: the project only runs where `requires:
# ["cxx"]` holds, and a future windows arm would add its own `-windows`
# archive under `[link.windows]`.
#
# The static path is relative to THIS project dir (the projects runner chdir's
# in before building): ../../../helpers -> <repo>/tests/helpers.

[project]
project_name = "ffi_cpp_link"
output_dir   = "build"
target_type  = "executable"
source_dir   = "src"
source_paths = ["bindings"]
entry_point  = "src/main.cryo"

[compiler]

[dependencies]

[link.unix]
static = ["../../../helpers/libcpplinkhelper-unix.a"]
system = ["stdc++"]
