/// Placeholder main.  `cryo test` suppresses this and synthesizes a test-runner
/// main from every `![test]` under this project's `tests/`.  The body exists
/// only to keep the executable target honest.
namespace FfiCppLink::Main;

import std::core::intrinsics;

function main(argc: int, argv: string*) -> int {
    intrinsics::printf("ffi_cpp_link: run with `cryo test`.\n");
    return 0;
}
