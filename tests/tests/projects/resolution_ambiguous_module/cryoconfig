# resolution_ambiguous_module - E0154 when a module-suffix scope is spelled by
# two modules the writing file can BOTH reach.
#
# Read src/main.cryo first. The answer is a diagnostic, not a tie-break: a
# resolver that picks one is choosing by something the source never wrote.

[project]
project_name = "resolution_ambiguous_module"
output_dir   = "build"
target_type  = "executable"
source_dir   = "src"
entry_point  = "src/main.cryo"

[compiler]

[dependencies]
