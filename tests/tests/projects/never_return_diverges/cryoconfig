# never_return_diverges - built and RUN end-to-end (outcome = "run"). A
# `-> never` function whose body ends in a divergent VOID call must reach
# codegen intact and must not return; the runner asserts the exit code the
# never-function passed to `exit` and the marker it printed on the way.

[project]
project_name = "never_return_diverges"
output_dir   = "build"
target_type  = "executable"
source_dir   = "src"
entry_point  = "src/main.cryo"

[compiler]

[dependencies]
