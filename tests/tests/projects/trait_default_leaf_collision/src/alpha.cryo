namespace TraitDefaultLeafCollision::Alpha;

import TraitDefaultLeafCollision::Shared;
import TraitDefaultLeafCollision::Shared::{ Feed };

type struct Cell { v: int; }

implement trait Feed<int> for struct Cell {
    seed(&this) -> int { return 11; }
}
