# async_main_params - `async function main` that declares the C entry-point
# parameters (outcome = "run").

[project]
project_name = "async_main_params"
output_dir   = "build"
target_type  = "executable"
source_dir   = "src"
entry_point  = "src/main.cryo"

[compiler]

[dependencies]
