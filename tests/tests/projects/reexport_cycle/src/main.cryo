/// `Bee` is declared in Beta and arrives through Alpha's re-export.
namespace ReexportCycle::Main;

import ReexportCycle::Alpha;

function main() -> int {
    const a: Ay  = Ay  { v: 5 };
    const b: Bee = Bee { v: 6 };
    return (a.get() + b.get()) as int;
}
