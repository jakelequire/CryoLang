# run_exit_and_stdout - a test PROJECT built and RUN end-to-end (outcome =
# "run"). The projects meta-runner runs `cryo run` here and asserts the
# program's exit code equals expect.exit_code AND its combined output contains
# expect.stdout_contains. This is the realistic-integration-test use case.

[project]
project_name = "run_exit_and_stdout"
output_dir   = "build"
target_type  = "executable"
source_dir   = "src"
entry_point  = "src/main.cryo"

[compiler]

[dependencies]
