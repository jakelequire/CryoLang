/// Built and run end-to-end by the projects meta-runner. Prints a known line
/// and exits with a known non-zero code; the runner asserts both.
namespace RunExitAndStdout::Main;

import std::core::intrinsics;

function main() -> int {
    intrinsics::printf("hello from run project\n");
    return 7;
}
