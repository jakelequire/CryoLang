/* Included BY c_types.h, never named in the import.  A library's entry header
 * is usually an umbrella of `#include`s that defines nothing itself, so a macro
 * filter keyed on the main file alone sees none of the constants the library
 * actually ships.  CIT_INNER is that case in miniature. */
#define CIT_INNER 7
