# visibility_gate - the §3.3 visibility gate, pinned through all THREE doors.
#
# Read src/main.cryo first. `cryo build` must FAIL here with one E0353 per
# door, and must NOT mention the public control `Vault::door`.
#
# This is the compile_fail half of the flip described in
# tests/tests/projects/resolution_tripwire: that project pinned "a private
# function is callable from another module" as a PASSING test because the
# compiler was wrong. The gate landed 2026-08-05, so the defect moved here as
# an error and the tripwire kept only its still-unfixed §5.1 half.

[project]
project_name = "visibility_gate"
output_dir   = "build"
target_type  = "executable"
source_dir   = "src"
entry_point  = "src/main.cryo"

[compiler]

[dependencies]
