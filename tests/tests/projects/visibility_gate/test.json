{
  "outcome": "compile_fail",
  "expect": {
    "diagnostic": "E0353",
    "output_contains": [
      "`VisibilityGate::Vault::latch` is private and cannot be called",
      "cannot find function `secret` in this scope",
      "cannot find function `stash` in this scope"
    ],
    "output_excludes": [
      "VisibilityGate::Vault::door"
    ]
  }
}
