# static_path_prefers_inherent_method - an inherent method and a trait impl's
# method share one name and one signature on one type.  `p.go()` binds the
# inherent method, and so does the path `P::go(&p)`: a `Type::method` path
# names the inherent item and a trait's is reached through the trait's own
# path, as in Rust.  Both answer 3 (`v * 3`), so the program exits 33; a
# selector that saw two entries of one signature as a tie left the call
# unpinned and codegen refused it (E0636), and one keyed by symbol bound
# whichever registered last.
[project]
project_name = "static_path_prefers_inherent_method"
output_dir   = "build"
target_type  = "executable"
source_dir   = "src"
entry_point  = "src/main.cryo"
[compiler]
[dependencies]
