/// Imports ONLY Blue.
![config(testing)]
namespace ResolutionScope::UsesBlue;

import ResolutionScope::Blue;
import ResolutionScope::Blue::{ Paint, tint };

function bare_call() -> i32 { return tint(); }

function bare_annotation() -> Paint { return Paint::mix(); }
