/// The colliding `Widget`. It carries a DIFFERENT field name, so a caller that
/// binds to this one by mistake fails to compile at the field access rather
/// than silently type-checking against the wrong struct.
![config(testing)]
namespace GenericNameCollision::WidgetOther;

type struct Widget<T> {
    other_v: T;
}
