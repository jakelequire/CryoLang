/// The colliding twin: same leaf name, same arity, different module, opposite
/// result (returns the LARGER argument).
![config(testing)]
namespace GenericNameCollision::Beta;

function pick<T>(a: T, b: T) -> T { if (a > b) { return a; } return b; }
