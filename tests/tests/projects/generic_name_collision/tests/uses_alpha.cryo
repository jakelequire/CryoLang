/// Imports ONLY Alpha, so its bare `pick` must bind to Alpha's template.
![config(testing)]
namespace GenericNameCollision::UsesAlpha;

import GenericNameCollision::Alpha;

function alpha_pick(a: i32, b: i32) -> i32 {
    const got: i32 = pick(a, b);
    return got;
}
