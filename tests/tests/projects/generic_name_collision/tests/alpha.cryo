/// One of two modules defining a generic `pick`. This one returns the SMALLER
/// argument, so a caller that binds to the wrong module's template gets a
/// visibly wrong VALUE rather than merely failing to compile.
![config(testing)]
namespace GenericNameCollision::Alpha;

function pick<T>(a: T, b: T) -> T { if (a < b) { return a; } return b; }
