/// Placeholder main. `cryo test` suppresses this and synthesizes a
/// test-runner main from every `![test]` under this project's `tests/`.
namespace GenericNameCollision::Main;

import std::core::intrinsics;

function main(argc: int, argv: string*) -> int {
    intrinsics::printf("generic_name_collision: run with `cryo test`.\n");
    return 0;
}
