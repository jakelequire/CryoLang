/// Declares the type the aggregate re-exports.
namespace ReexportBasic::Inner;

type struct Widget {
    v: i32;

    unwrap(&this) -> i32 { return this.v; }
}

function make_widget(n: i32) -> Widget {
    return Widget { v: n };
}
