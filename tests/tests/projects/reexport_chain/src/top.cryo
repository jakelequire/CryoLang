/// Hop 2. Names `Mid`, NOT `Deep`: this is what keeps a module's internal
/// layout its own. Moving `Widget` between Mid's children is invisible here.
namespace ReexportChain::Top;

export ReexportChain::Mid;
