# compile_fail_typeerror — a test PROJECT that must FAIL to compile with a
# specific diagnostic (outcome = "compile_fail"). The projects meta-runner
# runs `cryo build` here and asserts it fails emitting error[E0200]. This is
# the project-granularity lift of the file-level `![config(negative, E0xxx)]`
# mechanism — useful for exercising the module system / resolver negatively.

[project]
project_name = "compile_fail_typeerror"
output_dir   = "build"
target_type  = "executable"
source_dir   = "src"
entry_point  = "src/main.cryo"

[compiler]

[dependencies]
