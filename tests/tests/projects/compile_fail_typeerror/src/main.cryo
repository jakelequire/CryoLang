/// Deliberately ill-typed: `main` is declared to return `int` but the body
/// returns a string literal. The compiler must reject this with E0200
/// (mismatched types); the projects meta-runner asserts that diagnostic.
namespace CompileFailTypeError::Main;

function main() -> int {
    return "this is not an int";
}
