{
  "outcome": "compile_fail",
  "expect": {
    "diagnostic": "E0241",
    "output_contains": [
      "`Hidden` is private in `ReexportPrivate::Store`",
      "an export cannot widen visibility",
      "aborting due to 1 error"
    ],
    "output_excludes": [
      "`Shown` is private",
      "`Shown` is not declared"
    ]
  }
}
