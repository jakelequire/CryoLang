{
  "outcome": "compile_fail",
  "expect": {
    "diagnostic": "E0900",
    "output_contains": [
      "src/main.cryo:9:20",
      "the operand of `sizeof` reached codegen with no type decided"
    ],
    "output_excludes": [
      "Compiled ->"
    ]
  }
}
