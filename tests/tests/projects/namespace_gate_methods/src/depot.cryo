/// The declaring module. Only this module declares `Parcel`.
namespace NamespaceGateMethods::Depot;

type struct Parcel {
    n: i32;

    static of(n: i32) -> Parcel { return Parcel { n: n }; }
    get(&this) -> i32 { return this.n; }
}
