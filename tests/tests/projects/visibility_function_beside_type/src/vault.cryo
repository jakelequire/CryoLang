/// A public type and a private function declared under one leaf.  They are
/// two definitions, each with its own visibility.
namespace VisibilityFunctionBesideType::Vault;

public type struct latch {
    v: i32;
}

private function latch() -> i32 { return 7; }
