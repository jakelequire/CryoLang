{
  "outcome": "compile_fail",
  "expect": {
    "diagnostic": "E0353",
    "output_contains": [
      "`VisibilityFunctionBesideType::Vault::latch` is private and cannot be called from `VisibilityFunctionBesideType::Main`"
    ]
  }
}
