{
  "outcome": "compile_fail",
  "expect": {
    "diagnostic": "E0503",
    "output_contains": [
      "type `Hidden` is private to module `VisibilityTypeMask::Vault`"
    ],
    "output_excludes": [
      "`Shown` is private"
    ]
  }
}
