namespace ReexportPlural::Main;
import ReexportPlural::Facade;
import ReexportPlural::Left;
function main() -> int {
    const control: i32 = Left::make();
    const plural: i32 = Facade::make();
    return (control + plural) as int;
}
