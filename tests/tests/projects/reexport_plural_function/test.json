{
  "outcome": "compile_fail",
  "expect": {
    "diagnostic": "E0155",
    "output_contains": [
      "`make` is offered by `Facade` from more than one module, so it names nothing",
      "src/main.cryo",
      "ambiguous through this path",
      "aborting due to 1 error"
    ],
    "output_excludes": [
      "error[E0233]",
      "error[E0240]",
      "src/left.cryo",
      "src/right.cryo"
    ]
  }
}
