{
  "outcome": "compile_fail",
  "expect": {
    "diagnostic": "E0156",
    "output_contains": [
      "name the implementation the call is meant through: `(Beta for P)::go(&p)` or `(Gamma for P)::go(&p)`",
      "name the implementation the call is meant through: `(Beta for P)::make()` or `(Gamma for P)::make()`",
      "src/main.cryo:29:20",
      "src/main.cryo:30:20",
      "src/main.cryo:31:20"
    ],
    "output_excludes": [
      "no qualified form"
    ]
  }
}
