namespace stdlib::collections::pair;

// ============================================================================
// Pair
// ============================================================================

/// A tuple of two values.
type struct Pair<T, U> {
    first: T;
    second: U;

    /// Creates a new Pair with the given values.
    static new(first: T, second: U) -> Pair<T, U> {
        return Pair {
            first: first,
            second: second
        };
    }
    
    get_first(&this) -> T {
        return this.first;
    }

    get_second(&this) -> U {
        return this.second;
    }

    swap(&this) -> Pair<U, T> {
        return Pair<U, T> {
            first: this.second,
            second: this.first
        };
    }
}
