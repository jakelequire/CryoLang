// @test: if_else
// @expect: compile_success
// @mode: raw
// @stdout: positive
// @tags: control_flow, if_else

namespace IfElse;

function main() -> int {
    const x: int = 10;
    if (x > 0) {
        printf("positive\n");
    } else {
        printf("non-positive\n");
    }
    return 0;
}
