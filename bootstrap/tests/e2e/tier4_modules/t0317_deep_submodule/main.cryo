// @test: deep_submodule
// @expect: compile_success
// @mode: project
// @stdout: 42
// @tags: modules, project, import, submodule, nested

namespace Main;

import App;

function main() -> int {
    printf("%d\n", App::Core::Math::add(10, 32));
    return 0;
}
