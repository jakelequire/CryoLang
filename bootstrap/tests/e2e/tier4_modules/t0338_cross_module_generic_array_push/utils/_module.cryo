namespace Utils;

type struct Bag<T> {
    items: T[];
    size: int;

    static new() -> Bag<T> {
        return Bag {
            items: [],
            size: 0,
        };
    }

    add(item: T) -> void {
        this.items.push(item);
        this.size = this.size + 1;
    }
}
