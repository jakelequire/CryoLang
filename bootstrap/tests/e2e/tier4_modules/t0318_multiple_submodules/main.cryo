// @test: multiple_submodules
// @expect: compile_success
// @mode: project
// @stdout: 42
// @stdout: Hello!
// @tags: modules, project, import, submodule, multi

namespace Main;

import Lib;

function main() -> int {
    printf("%d\n", Lib::Math::add(10, 32));
    Lib::Strings::greet();
    return 0;
}
