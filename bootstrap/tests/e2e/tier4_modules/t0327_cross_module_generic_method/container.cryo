namespace Container;

type struct Box<T> {
    value: T;

    static new(val: T) -> Box<T> {
        return Box { value: val };
    }

    get(&this) -> T {
        return this.value;
    }

    set(mut &this, val: T) -> void {
        this.value = val;
    }
}
