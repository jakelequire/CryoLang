type union Box {
    i: i64;
    f: f64;
}

implement union Box {
    as_int(&this) -> i64 { return this.i; }
    static make(n: i64) -> Box { return Box { i: n }; }
}

function main() -> i32 {
    const b: Box = Box::make(55);
    printf("impl_union=%lld\n", b.as_int());
    return 0;
}
