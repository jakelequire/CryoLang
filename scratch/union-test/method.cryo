type union Value {
    i: i64;
    f: f64;

    get_i(&this) -> i64 {
        return this.i;
    }
}

function main() -> i32 {
    mut v: Value = Value { i: 42 };
    printf("get_i = %lld\n", v.get_i());
    return 0;
}
