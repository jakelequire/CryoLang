# cpp-demo consumer — drives the GENERATED C++ bindings (vendor::CppTest),
# proving the full `cryo vendor` → import → link → run path for C++ Strategy A.
# The [link] is intentionally empty: the static libcpptest.a and -lstdc++ are
# auto-injected from the vendor registry entry.
[project]
project_name = "cpp_consumer"
output_dir   = "build"
target_type  = "executable"
source_dir   = "src"
entry_point  = "src/main.cryo"

[compiler]

[dependencies]

[link]
