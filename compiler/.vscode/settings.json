{
    // No cryo.languageServer.path here on purpose: the CryoAnalyzer
    // extension auto-detects the LSP binary relative to `cryo` on PATH
    // (see tools/CryoAnalyzer/src/config.ts). Hardcoding an absolute
    // path breaks across machines / Linux+Windows checkouts.
}
