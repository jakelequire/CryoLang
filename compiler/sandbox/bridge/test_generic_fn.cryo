/// Test: generic free function call with explicit args.
namespace test_generic_fn;

function identity<T>(x: T) -> T {
    return x;
}

function main() -> i32 {
    const y: i32 = identity<i32>(42);
    return y;
}
