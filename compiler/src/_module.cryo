/// Compiler library: top-level discovery anchor.
///
/// `[lib].source_dir = "src"` points discovery at this file.  We import
/// each top-level namespace under src/ so the loader's transitive walk
/// reaches the whole tree.  Adding a new top-level under src/ means
/// adding an import line here.

namespace project::compiler;

import compiler;
import utils;
