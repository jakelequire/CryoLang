///! Vendor Library System
///!
///! Per-machine registry of vendored C libraries plus the `import vendor::Name`
///! resolution path.  See `registry.cryo` for the on-disk format and the
///! resolution/link-merge entry points.

namespace compiler::vendor;

public module Registry;
public module AutoVendor;
