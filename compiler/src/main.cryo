namespace main;

import cli;
import utils::logger;

const VERSION: string = "1.0.0";

function main(argc: int, argv: string*) -> int {
    Logger::init();

    const runner: cli::Runner = cli::Runner::new("cryo", VERSION)
                                .init_commands();
    return runner.run(argc, argv);
}
