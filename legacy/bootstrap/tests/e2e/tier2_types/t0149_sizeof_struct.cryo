// @test: sizeof_struct
// @expect: compile_success
// @mode: raw
// @stdout_contains: 8
// @tags: types, sizeof, structs

namespace SizeofStruct;

type struct Pair {
    x: i32;
    y: i32;
}

function main() -> int {
    const size: u64 = sizeof(Pair);
    printf("%lu\n", size);
    return 0;
}
