namespace Utils;

type struct Container<T> {
    items: T[];

    static new() -> Container<T> {
        return Container {
            items: [],
        };
    }

    add(item: T) -> void {
        this.items.push(item);
    }

    size() -> int {
        return this.items.length;
    }
}
