// @test: import_multiple_modules
// @expect: compile_success
// @mode: project
// @stdout: 42
// @tags: modules, project, import, multi

namespace Main;

import Foo;
import Bar;

function main() -> int {
    printf("%d\n", Foo::get_x() + Bar::get_y());
    return 0;
}
