// @test: cross_module_generic_field_method_call
// @expect: compile_success
// @mode: project
// @stdout: done
// @tags: modules, project, generics, cross_module, field_method_call
// @description: Non-generic struct with a generic-typed field from an imported module; calling methods on that field

namespace Main;

import Store;

function main() -> int {
    const reg: Store::Registry = Store::Registry::new();
    reg.add("foo", 42);
    printf("done\n");
    return 0;
}
