// @test: import_wrong_member
// @expect: compile_fail
// @mode: project
// @tags: modules, project, import, negative, structs

namespace Main;

import Geo;

function main() -> int {
    const p: Geo::Point = Geo::Point::new(10, 32);
    printf("%d\n", p.z);
    return 0;
}
