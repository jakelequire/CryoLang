namespace Counter;

type struct Ctr {
    val: int;

    static new() -> Ctr {
        return Ctr { val: 0 };
    }

    increment(mut &this) -> void {
        this.val = this.val + 1;
    }

    get(&this) -> int {
        return this.val;
    }
}
