namespace Testing;

import <io/stdio>;
import <core/types>;

function main() -> int {
    const s: string = "hello";
    const len: u64 = s.length();
    std::IO::print_int(len);

    std::IO::print_int("Hello, World!".length());
    
    return 0;
}