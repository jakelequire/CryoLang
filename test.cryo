namespace Testing;

import IO from <io/stdio>;

extern function foo() -> int;

type ExitHandler     =  () -> void;         // Standard function type syntax
type GenericFunction =  <T>(arg: T) -> T;   // Generic function type

mut test_global: int = 42;

function main() -> int {
    IO::printf("Hello, world!\n");
    
    IO::printf("Test global:\n");
    IO::print_int(test_global);
    
    return 0;
}
