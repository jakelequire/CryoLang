namespace NbOverlap::Main;

import std::core::intrinsics;
import NbOverlap::Shapes;
import NbOverlap::Bound;

function main(argc: int, argv: string*) -> int {
    Shapes::run();
    Bound::run();
    return 0;
}
