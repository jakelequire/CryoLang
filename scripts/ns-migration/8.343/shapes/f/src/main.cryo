namespace Main;

import std::collections::array::{ Array };
import std::core::default::{ Default };

function main() -> int {
    mut a: Array<i32> = Array::<i32>::default();
    a.push(5);
    return a.length() as int;
}
