namespace Nostamp::Main;

type struct Point {
    x: i32;
    y: i32;

    sum(&this) -> i32 {
        return this.x + this.y;
    }
}

function main() -> int {
    const p: Point = Point { x: 4, y: 3 };
    return p.sum();
}
