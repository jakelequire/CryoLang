diff --git a/compiler/src/compiler/AST/_module.cryo b/compiler/src/compiler/AST/_module.cryo
index a7082f39..f638cf53 100644
--- a/compiler/src/compiler/AST/_module.cryo
+++ b/compiler/src/compiler/AST/_module.cryo
@@ -26,6 +26,7 @@ import compiler::resolver::symbol_str;
 import compiler::resolver::symbol_str::{ SymbolStr };
 import compiler::resolver::res;
 import compiler::resolver::res::{ ResSlot };
+import std::fmt;
 import compiler::ast::expression;
 import compiler::ast::expression::{ ExpressionNode };
 
@@ -672,7 +673,8 @@ type struct TraitRef {
     /// per call site.
     identity(&this) -> SymbolStr {
         if (this.resolved_name.is_valid()) { return this.resolved_name; }
-        if (this.path.length == 0) { return SymbolStr::empty(); }
+        if (this.path.length == 0) { fmt::eprintf("SHADOW\tTRID\tnopath\t-\n"); return SymbolStr::empty(); }
+        fmt::eprintf("SHADOW\tTRID\tleaf\t%llu\t%lld\n", this.path[this.path.length - 1].id as u64, this.path.length);
         return this.path[this.path.length - 1];
     }
 
