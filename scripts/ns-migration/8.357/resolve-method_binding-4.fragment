    param_types_through_param_bounds(mut &this, param_ty: TypeRef, method_name: SymbolStr,
                                     arg_count: i64) -> TypeRef[] {
        const param: GenericParamType* = this.param_name_through_impl_head(param_ty);
        if (param == null) { return []; }
