diff --git a/compiler/src/compiler/compilation_context.cryo b/compiler/src/compiler/compilation_context.cryo
index 0e6b3355..970465ba 100644
--- a/compiler/src/compiler/compilation_context.cryo
+++ b/compiler/src/compiler/compilation_context.cryo
@@ -302,6 +302,7 @@ public:
         graph_ptr.defs = defs_ptr;
         const decl_ptr: DeclarationIndex* = _box_decl.leak();
         decl_ptr.defs = defs_ptr;
+        if (env::var("CRYO_FAMPROBE").is_some()) { decl_ptr.probe_intern = intern_ptr; }
         const consts_ptr: ConstantTable* = _box_consts.leak();
         consts_ptr.defs = defs_ptr;
 
diff --git a/compiler/src/compiler/decl_index.cryo b/compiler/src/compiler/decl_index.cryo
index 914b7aa1..b890c434 100644
--- a/compiler/src/compiler/decl_index.cryo
+++ b/compiler/src/compiler/decl_index.cryo
@@ -79,6 +79,7 @@ type struct DeclarationIndex {
     /// The compilation's definitions, for a definition's path.  Borrowed;
     /// set by `CompilationContext`, which owns it.
     defs: DefTable*;
+    probe_intern: InternTable*;
     // Function name -> FunctionType TypeRef (full signature in the arena)
     func_type_refs: HashMap<u32, TypeRef>;
 
@@ -243,6 +244,7 @@ type struct DeclarationIndex {
     static new() -> DeclarationIndex {
         return DeclarationIndex {
             defs: null,
+            probe_intern: null,
             func_type_refs:      HashMap::<u32, TypeRef>::new(),
             method_returns:      HashMap::<u64, TypeRef>::new(),
             type_map:            HashMap::<u32, TypeRef>::new(),
@@ -340,6 +342,8 @@ type struct DeclarationIndex {
     register_signature(mut &this, name: SymbolStr, func_type_ref: TypeRef,
                        owner: SymbolStr, symbol: SymbolStr, trait_sym: DefId,
                        span: SourceSpan) -> OverloadId {
+        if (this.probe_intern != null) { fmt::eprintf("FAMREG	%s
+", this.probe_intern.resolve(name)); }
         this.func_type_refs.insert(name.id, func_type_ref);
         match (this.overload_index.get_ref(&name.id)) {
             Option::Some(positions) => {
@@ -400,6 +404,8 @@ type struct DeclarationIndex {
 
     /// Every entry registered under key `name`, in registration order.
     lookup_family_entries(&this, name: SymbolStr) -> OverloadId[] {
+        if (this.probe_intern != null) { fmt::eprintf("FAMGET	entries	%s
+", this.probe_intern.resolve(name)); }
         mut result: OverloadId[] = [];
         match (this.overload_index.get_ref(&name.id)) {
             Option::Some(positions) => {
@@ -454,6 +460,8 @@ type struct DeclarationIndex {
     /// The signature of every entry under `name`, in registration order
     /// (`lookup_family_entries` gives the entries themselves).
     lookup_func_type_overloads(&this, name: SymbolStr) -> TypeRef[] {
+        if (this.probe_intern != null) { fmt::eprintf("FAMGET	overloads	%s
+", this.probe_intern.resolve(name)); }
         mut result: TypeRef[] = [];
         match (this.overload_index.get_ref(&name.id)) {
             Option::Some(positions) => {
@@ -852,6 +860,8 @@ type struct DeclarationIndex {
     /// function's return type is this signature's; there is no second map
     /// keyed by the same name that could answer differently.
     lookup_func_type(&this, name: SymbolStr) -> TypeRef {
+        if (this.probe_intern != null) { fmt::eprintf("FAMGET	functype	%s
+", this.probe_intern.resolve(name)); }
         return match (this.func_type_refs.get(&name.id)) {
             Option::Some(val) => { val }
             Option::None      => { TypeRef::invalid() }
