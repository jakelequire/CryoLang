diff -ru a/compiler/src/compiler/resolver/resolver.cryo b/compiler/src/compiler/resolver/resolver.cryo
--- a/compiler/src/compiler/resolver/resolver.cryo	2026-09-18 01:13:51.915350800 -0700
+++ b/compiler/src/compiler/resolver/resolver.cryo	2026-09-18 01:23:37.816985100 -0700
@@ -17,6 +17,7 @@
 import compiler::diag::source_span;
 import compiler::diag::source_span::{ SourceSpan };
 import std::collections::{ pair, hashmap };
+import std::fmt;
 import std::core::option;
 import std::core::option::{ Option };
 import utils::logger;
@@ -651,6 +652,14 @@
                     // head - to nothing - so the walk stops here rather than
                     // letting an outer tier answer a name this one holds.
                     if (sym.kind == SymbolKind::Import && sc.is_ambiguous(head)) {
+                        {
+                            // SHADOW: every head a tied import binds, with the two modules.
+                            const mods: u32[] = sc.get_ambiguous_module_ids(head);
+                            fmt::eprintf("SHADOW\tAMBPATH\t%s\t%s\t%s\t%s\n",
+                                this.intern_table.resolve(head), sc.module_name,
+                                if (mods.length > 0) { this.intern_table.resolve(SymbolStr::from_id(mods[0])) } else { "" },
+                                if (mods.length > 1) { this.intern_table.resolve(SymbolStr::from_id(mods[1])) } else { "" });
+                        }
                         return Res::Err;
                     }
                     return this.res_for_head(&sym, trailing);
