diff -ru a/compiler/src/compiler/sema/async_lower.cryo b/compiler/src/compiler/sema/async_lower.cryo
--- a/compiler/src/compiler/sema/async_lower.cryo	2026-09-17 23:48:17.291278700 -0700
+++ b/compiler/src/compiler/sema/async_lower.cryo	2026-09-17 23:48:24.757181000 -0700
@@ -2480,6 +2480,18 @@
         // loop then found nothing.
         if (this.ctx.generic_registry == null) { return false; }
         const tdecl: TraitDeclNode* = this.ctx.generic_registry.trait_decl_of(ap.owning_trait);
+        {
+            // SHADOW: the leaf scan beside the identity, one line per reader call.
+            const old_id: SymbolStr = this.ctx.generic_registry.find_trait_defining_method(mname);
+            const old_decl: TraitDeclNode* = this.ctx.generic_registry.get_trait_decl(old_id);
+            const sp: SourceSpan = (e as ASTNode*).span;
+            fmt::eprintf("SHADOW\tRECVTRAIT\t%s:%u\t%s\told=%s\tnew=%s\t%s\tot_valid=%d\n",
+                sp.file, sp.start_line, this.intern.resolve(mname),
+                if (old_id.is_valid()) { this.intern.resolve(old_id) } else { "<none>" },
+                if (tdecl != null) { this.intern.resolve(tdecl.name) } else { "<null>" },
+                if (tdecl == null) { "NEWNULL" } else { if (old_decl == tdecl) { "SAME" } else { "DIFF" } },
+                if (ap.owning_trait.is_valid()) { 1 } else { 0 });
+        }
         if (tdecl == null) { return false; }
         for (mut i: i64 = 0; i < tdecl.methods.length; i++) {
             const f: FunctionDeclNode* = tdecl.methods[i];
diff -ru a/compiler/src/compiler/types/arena.cryo b/compiler/src/compiler/types/arena.cryo
--- a/compiler/src/compiler/types/arena.cryo	2026-09-17 23:48:17.390399900 -0700
+++ b/compiler/src/compiler/types/arena.cryo	2026-09-17 23:48:28.933502200 -0700
@@ -1225,6 +1225,9 @@
             }
             TypeKind::Trait => {
                 const tt: TraitType* = t as TraitType*;
+                // SHADOW: every ask that names a trait, so the count of asks the
+                // new arm answers can be set against the reader's own.
+                fmt::eprintf("SHADOW\tGQN-TRAIT\t%s\n", this.intern_table.resolve(tt.qualified_name));
                 return tt.qualified_name;
             }
             TypeKind::InstantiatedType => {
