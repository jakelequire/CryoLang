# D32's residue: every name-keyed read in `compiler/src`, classified

The completion criterion for merging `naming-impl` (`docs/name-resolution.md`
§0.1 D32, §8.276): **zero name-keyed reads outside an enumerated, justified
residue, pinned by count.** This file is the enumeration. It is one list;
every site carries its class and a one-line reason, and the J rows are the
residue Jake rules on.

## How the list is made, and how it is checked

* **The population is derived, not recalled.** `residue.py` builds it from
  `scripts/lane-gate.py`'s own parser - the stores rule 1 places, the array
  owners rule 1b places as *a member table inside its owner*, rule 2's
  signature test narrowed to a key type in PARAMETER position on a read
  (`&this` or static), and the receiver placed by its declared type (a
  dotted receiver by its annotation, a cast receiver by the type the cast
  names, a static by its spelled owner, turbofish included). A
  reader added under any spelling on any store is in the population the
  moment it is declared; a type excluded by the gate for holding paths,
  flags, texts or a pass's own rib (`DiagSink`, `ParsedArgs`,
  `ProjectConfig`, `AsyncLower`, ...) holds no declaration and is outside.
  **The same read written INLINE is a site too** (rule 1c): a loop that
  compares an array element's key-typed field with a spelling -
  `st.methods[i].name.equals(n)`, a local bound to the element
  (`const m: MethodNode* = d.methods[i]; .. m.func.name.equals(n)`), a
  `.name.id ==` compare, an array of bare names (`names[i].equals(n)`), a
  LOCAL array of records (`slots[ki].name.equals(n)`) - is the door's body
  written at the caller, and it was invisible while the population was
  method calls alone. The gate places every scanned (owner, array) as a
  TABLE (a declaration table by leaf: in the population, the row's read
  spelled `Owner::field[]`, a local table `local::Elem[]`) or as DATA
  (texts, flags, paths, triples, a dedupe set: outside), and refuses one
  in neither; a scan in the owner's own file is the door's body, as a
  store's own calls are.
  `python scripts/ns-migration/residue.py --count` is the size;
  without a flag it prints every site as `file`, `line`, receiver type,
  method, argument text.
* **The classes are judgement, held as code.** `residue_classify.py` maps
  each read METHOD to a class and a reason, with a per-site override where
  one call's key comes from somewhere the method's other callers' do not,
  and writes the table below. A method it classifies that the tree no
  longer declares, a read method the tree calls that it does not classify,
  or an override matching no call, each refuse the run.
* **The check.** `python scripts/ns-migration/residue.py --check` matches
  the table to the tree by (file, method, key text) - a line shift is not
  drift, a site added, removed or re-keyed is - and re-derives every row's
  class from `residue_classify.py`, refusing a row whose letter is not the
  classifier's (flipped, blanked, or a letter the classes do not include);
  on OK it prints the count per class from the classifier, so the number
  §0 pins is checked against the tree, the list and the code that decides
  it. A check that read the letters as written let a J row flipped to N by
  hand pass with J one lower. It also refuses a call to a population name
  that no receiver pattern reaches (a parenthesized receiver, a call with
  arguments), and pins the *elsewhere* table below. `residue_selftest.py`
  (`make residue-selftest`, in `check-fast` and CI) drives each refusal
  through a throwaway tree and list. §0's D32 row carries the command and
  the counts.
* The table is regenerated by `python scripts/ns-migration/residue_classify.py`;
  a run that changes a row's class is a change of judgement and goes in
  the ledger with its reason.

## The classes

| class | meaning | in the residue? |
|---|---|---|
| **J** | **justified** - name-keyed by design; the sub-kind is the reason's first word: `member` (a member's leaf inside a settled owner - Rust's field and method lookup is name-keyed off the owner too), `lang` (a declaration the language names by its own spelling and claims at declaration - Rust's lang items are keyed by the attribute's string), `module` (a module by its namespace path - the module graph is the declaration of modules and a namespace names exactly one, D5), `prim` (a primitive by the spelling its `Res::PrimTy` stamp carries), `extern` (a C symbol by its link name), `hint` (a did-you-mean whose question is the spelling) | **yes - these are the count** |
| N | not a key - the string names no declaration: a diagnostic label beside a `DefId`, a file path, a literal's text, a constructor's source file, a write onto an AST node, a funnel's own forwarding body. In the population because the gate's parameter test cannot tell a label from a key | no - outside D32 |
| S | stamp-derived - an index or funnel door keyed by a canonical string the caller derived from a stamp or a `TypeRef`. Convertible: the door can take the stamp | no - convertible, remaining work |
| B | the `GenericRegistry` keyed by the canonical string of a stamped identity - bucket B's reader side (§8.245); re-keying the registry by identity is Jake's unit | no - convertible, Jake's unit |
| C | a key composed from parts (`owner::member`, a family string, `ns::name`) - bucket C, behind pin-as-declaration | no - convertible, blocked |
| F | visibility asked by the callee's name (`enforce_callee_visibility`) - bucket F, behind pin-as-declaration | no - convertible, blocked |
| W | a written spelling with no stamp behind it and no reason above | no - must convert |

**The migration is complete when S, B, C, F and W are all 0 and J is at or
below the ceiling Jake sets.** The J sub-kinds are separable: if `module`
(a module by its path) or `lang` (a language-named declaration by its
spelling) is ruled NOT by design, the rows are already grouped for it and
the ceiling moves down by that sub-kind's count; nothing here pads or
trims toward a number.

Two shapes the ledger named for this list before it existed, and where
they landed: the member table inside its owner (`ImplBlockNode::lookup_assoc_binding`,
`ResolutionContext::lookup_assoc_binding`, `TraitType::get_method`) is J
`member` - `get_method` has no caller outside its file, so it has no row;
`TemplateEntry.param_names`' spelling compares in `call_resolver.cryo`
are `equals` calls on an array, not a method of a holder, so they are not
in this population - they are §8.275's arena unit and retire with it.

A third shape the first list could not see: the member tables of the
user-defined types themselves. `StructType.fields: FieldInfo[]`,
`ClassType.methods: MethodInfo[]`, `EnumType.variants: EnumVariantInfo[]`
and `TraitDeclNode.assoc_types` are arrays of RECORDS with the name in
each, searched by `rows[i].name.equals(n)` - neither a map nor an array
of names, so no rule reached them and their 43 callers were nobody's.
The gate's rule 1b now takes an array of records carrying a key-typed
field as a candidate (37 owners on this tree, 20 placed as exclusions by
reason, four of them member tables), and those callers are J `member` -
a struct's field or method, an enum's variant, a trait's associated type,
each by its leaf off the type already in hand - or J `lang` where the leaf
is one the language fixes (`__call__`, `Ready`, `Ok`/`Err`, `Some`/`None`).

## The table

<!-- residue-table:begin -->
Population **321** - J 194 · N 127 · S 0 · B 0 · C 0 · F 0 · W 0

| site | read | key as written | class | reason |
|---|---|---|---|---|
| `compiler/AST/node_locator.cryo:1843` | `ClassDeclNode::fields[]` | `field_name` | J | member: a class declaration's field by its leaf off the node in hand, scanned inline |
| `compiler/sema/member_resolver.cryo:418` | `ClassDeclNode::fields[]` | `field` | J | member: a class declaration's field by its leaf off the node in hand, scanned inline |
| `compiler/sema/symbolic_checker.cryo:263` | `ClassDeclNode::fields[]` | `field` | J | member: a class declaration's field by its leaf off the node in hand, scanned inline |
| `compiler/types/ownership.cryo:1031` | `ClassDeclNode::methods[]` | `method_name` | J | member: a class declaration's method by its leaf off the node in hand, scanned inline |
| `compiler/sema/member_resolver.cryo:450` | `ClassType::field_index` | `field_name` | J | member: a class's field position by its leaf off the `ClassType` in hand |
| `compiler/types/checker.cryo:1088` | `ClassType::field_index` | `field_name` | J | member: a class's field position by its leaf off the `ClassType` in hand |
| `compiler/sema/diagnostics.cryo:312` | `ClassType::get_method` | `member_name` | J | member: a class's method by its leaf off the `ClassType` in hand |
| `compiler/sema/member_resolver.cryo:714` | `ClassType::get_method` | `method_name` | J | member: a class's method by its leaf off the `ClassType` in hand |
| `compiler/sema/sema.cryo:3195` | `ClassType::get_method` | `scope.member_name` | J | member: a class's method by its leaf off the `ClassType` in hand |
| `compiler/sema/sema.cryo:3212` | `ClassType::get_method` | `scope.member_name` | J | member: a class's method by its leaf off the `ClassType` in hand |
| `compiler/codegen/ops/declaration_emitter.cryo:781` | `ClassType::methods[]` | `slot_name` | J | member: a class's method by its leaf off the `ClassType` in hand, scanned inline (the vtable's slot walk; an arity predicate) |
| `compiler/sema/call_resolver.cryo:449` | `ClassType::methods[]` | `member.member` | J | member: a class's method by its leaf off the `ClassType` in hand, scanned inline (the vtable's slot walk; an arity predicate) |
| `compiler/passes/type_resolution.cryo:2483` | `DeclarationIndex::extern_symbol_conflict` | `ext_link_sym, func_type_ref` | J | extern: a C symbol by its link name, the only identity a C symbol has |
| `compiler/codegen/visit/call_emitter.cryo:1138` | `DeclarationIndex::family_intrinsic_kind` | `o, l` | J | member: the intrinsic kind an overload family's entries share, asked as `lookup_family_entries` is (the `CalleePin::Family` arm; the `Decl` arm reads the kind off the entry) |
| `compiler/sema/type_utils.cryo:154` | `DeclarationIndex::global_entry_in_module` | `scope.member_name, seen.def_id()` | J | member: a global's leaf inside the module the qualifier's stamp names (a `DefId`) |
| `compiler/compilation_context.cryo:543` | `DeclarationIndex::is_prelude_ns` | `cand.path()` | J | module: a namespace asked whether it is the prelude's; a module's identity is its path |
| `compiler/resolver/name_resolution.cryo:1513` | `DeclarationIndex::is_prelude_ns` | `owner_path` | J | module: a namespace asked whether it is the prelude's; a module's identity is its path |
| `compiler/codegen/ops/symbol_resolver.cryo:237` | `DeclarationIndex::lookup_family_entries` | `owner, leaf` | J | member: an overload family's leaf asked inside the owner the caller holds by identity - a module's or a type's definition, or a type's arena id (`FamilyOwner`); the store is keyed by that owner's path and the leaf (`family_slot`), as a definition is its parent and its leaf. Overloading is kept, so a family is the SET one owner declares under one written leaf, and the leaf is the question asked, not a stand-in for an identity - Rust's resolver keys the same set by parent module, ident and namespace |
| `compiler/sema/call_resolver.cryo:2486` | `DeclarationIndex::lookup_family_entries` | ` ...` | J | member: an overload family's leaf asked inside the owner the caller holds by identity - a module's or a type's definition, or a type's arena id (`FamilyOwner`); the store is keyed by that owner's path and the leaf (`family_slot`), as a definition is its parent and its leaf. Overloading is kept, so a family is the SET one owner declares under one written leaf, and the leaf is the question asked, not a stand-in for an identity - Rust's resolver keys the same set by parent module, ident and namespace |
| `compiler/sema/call_resolver.cryo:3732` | `DeclarationIndex::lookup_family_entries` | `FamilyOwner::Type(owner), member_sym` | J | member: an overload family's leaf asked inside the owner the caller holds by identity - a module's or a type's definition, or a type's arena id (`FamilyOwner`); the store is keyed by that owner's path and the leaf (`family_slot`), as a definition is its parent and its leaf. Overloading is kept, so a family is the SET one owner declares under one written leaf, and the leaf is the question asked, not a stand-in for an identity - Rust's resolver keys the same set by parent module, ident and namespace |
| `compiler/sema/call_resolver.cryo:3817` | `DeclarationIndex::lookup_family_entries` | `owner, member_sym` | J | member: an overload family's leaf asked inside the owner the caller holds by identity - a module's or a type's definition, or a type's arena id (`FamilyOwner`); the store is keyed by that owner's path and the leaf (`family_slot`), as a definition is its parent and its leaf. Overloading is kept, so a family is the SET one owner declares under one written leaf, and the leaf is the question asked, not a stand-in for an identity - Rust's resolver keys the same set by parent module, ident and namespace |
| `compiler/sema/call_resolver.cryo:4070` | `DeclarationIndex::lookup_family_entries` | `owner, leaf` | J | member: an overload family's leaf asked inside the owner the caller holds by identity - a module's or a type's definition, or a type's arena id (`FamilyOwner`); the store is keyed by that owner's path and the leaf (`family_slot`), as a definition is its parent and its leaf. Overloading is kept, so a family is the SET one owner declares under one written leaf, and the leaf is the question asked, not a stand-in for an identity - Rust's resolver keys the same set by parent module, ident and namespace |
| `compiler/sema/call_resolver.cryo:4188` | `DeclarationIndex::lookup_family_entries` | `owner, leaf` | J | member: an overload family's leaf asked inside the owner the caller holds by identity - a module's or a type's definition, or a type's arena id (`FamilyOwner`); the store is keyed by that owner's path and the leaf (`family_slot`), as a definition is its parent and its leaf. Overloading is kept, so a family is the SET one owner declares under one written leaf, and the leaf is the question asked, not a stand-in for an identity - Rust's resolver keys the same set by parent module, ident and namespace |
| `compiler/sema/call_resolver.cryo:931` | `DeclarationIndex::lookup_func_type` | `fam_owner, fam_leaf` | J | member: the signature last registered in an overload family, asked as `lookup_family_entries` is |
| `compiler/sema/call_resolver.cryo:1852` | `DeclarationIndex::lookup_func_type_overloads` | ` ...` | J | member: an overload family's signatures, asked as `lookup_family_entries` is |
| `compiler/sema/call_resolver.cryo:5691` | `DeclarationIndex::lookup_func_type_overloads` | `owner, leaf` | J | member: an overload family's signatures, asked as `lookup_family_entries` is |
| `compiler/passes/type_resolution.cryo:1863` | `DeclarationIndex::lookup_type` | `n` | J | prim: `Res::PrimTy(n)` - the primitive's identity is the spelling the stamp carries |
| `compiler/sema/call_resolver.cryo:3334` | `DeclarationIndex::method_signature_refused` | ` ...` | J | member: whether a method's written signature was refused, by its leaf off the owner `TypeRef` in hand; keyed as `lookup_method_return` is |
| `compiler/sema/call_resolver.cryo:3660` | `DeclarationIndex::method_signature_refused` | `owner_t, scope.member_name` | J | member: whether a method's written signature was refused, by its leaf off the owner `TypeRef` in hand; keyed as `lookup_method_return` is |
| `compiler/compilation_context.cryo:544` | `DeclarationIndex::ns_imports` | `use_module, cand.path()` | J | module: two namespaces asked whether one imports the other; module identities |
| `compiler/resolver/name_resolution.cryo:1514` | `DeclarationIndex::ns_imports` | `this.ctx.module_graph.module_of_file(span.file), owner_path` | J | module: two namespaces asked whether one imports the other; module identities |
| `compiler/resolver/name_resolution.cryo:1064` | `DestructureDeclNode::bindings[]` | `b.local_name.id` | J | member: the resolver's duplicate-binding check, a binding by its local's leaf inside the destructure being bound |
| `compiler/sema/sema.cryo:1422` | `DestructureDeclNode::bindings[]` | `b.source_field.id` | J | member: a destructure's written binding by the source field's leaf (which binding takes a field), scanned inline |
| `compiler/sema/sema.cryo:1467` | `DestructureDeclNode::bindings[]` | `fields_ptr[k].name.id` | J | member: a destructure's written binding by the source field's leaf (which binding takes a field), scanned inline |
| `compiler/const_table.cryo:579` | `EnumDeclNode::variants[]` | `member.id` | J | member: an enum declaration's variant by its leaf off the node in hand, scanned inline |
| `compiler/sema/diagnostics.cryo:317` | `EnumType::get_method` | `member_name` | J | member: an enum's method by its leaf off the `EnumType` in hand |
| `compiler/sema/sema.cryo:3292` | `EnumType::get_method` | `scope.member_name` | J | member: an enum's method by its leaf off the `EnumType` in hand |
| `compiler/sema/diagnostics.cryo:317` | `EnumType::get_variant` | `member_name` | J | member: an enum's variant by its leaf off the `EnumType` in hand |
| `compiler/sema/sema.cryo:2726` | `EnumType::get_variant` | `this.intern.intern("Ready")` | J | lang: `Poll::Ready`, the variant the async protocol names, inside the enum a synthesized `poll` returns - the enum matched to the language's `Poll` by the identity its declaration claimed before the variant is read |
| `compiler/sema/sema.cryo:3769` | `EnumType::get_variant` | `leaf` | J | member: an enum's variant by its leaf off the `EnumType` in hand |
| `compiler/sema/call_resolver.cryo:461` | `EnumType::methods[]` | `member.member` | J | member: an enum's method by its leaf off the `EnumType` in hand, scanned inline with an arity predicate |
| `compiler/sema/call_resolver.cryo:538` | `EnumType::variant_index` | `scope.member_name` | J | member: an enum's variant ordinal by its leaf off the `EnumType` in hand - sema's one lookup, pinned on the node for codegen |
| `compiler/sema/pattern_resolver.cryo:178` | `EnumType::variant_index` | `enum_pat.variant_name` | J | member: an enum's variant ordinal by its leaf off the `EnumType` in hand - sema's one lookup, pinned on the node for codegen |
| `compiler/sema/sema.cryo:3284` | `EnumType::variant_index` | `scope.member_name` | J | member: an enum's variant ordinal by its leaf off the `EnumType` in hand - sema's one lookup, pinned on the node for codegen |
| `compiler/sema/sema.cryo:3325` | `EnumType::variant_index` | `new_expr.variant_name` | J | member: an enum's variant ordinal by its leaf off the `EnumType` in hand - sema's one lookup, pinned on the node for codegen |
| `compiler/AST/node_locator.cryo:231` | `ExternBlockNode::functions[]` | `name` | J | member: an extern block's function by its leaf off the block in hand, scanned inline |
| `compiler/bindgen/importer.cryo:1775` | `ExternBlockNode::functions[]` | `sym` | J | extern: whether this extern block already declares the C function of this LINK NAME, asked as the importer emits it. A C symbol's link name is the whole of its identity - C has no declaration to key on, and two C declarations sharing a link name are one entity by the linkage rule - so there is no stamp this could ask instead, and the leaf it compares is the mangling the linker will use |
| `compiler/bindgen/importer.cryo:1847` | `ExternBlockNode::functions[]` | `sym` | J | extern: whether this extern block already declares the C function of this LINK NAME, asked as the importer emits it. A C symbol's link name is the whole of its identity - C has no declaration to key on, and two C declarations sharing a link name are one entity by the linkage rule - so there is no stamp this could ask instead, and the leaf it compares is the mangling the linker will use |
| `compiler/resolver/name_resolution.cryo:618` | `FunctionDeclNode::parameters[]` | `param.name.id` | J | member: the resolver's duplicate-parameter check, a parameter by leaf inside the function being bound |
| `compiler/mono/call_specializer.cryo:912` | `GenericRegistry::find_inherent_impl_generic_method` | ` ...` | J | member: a generic method's leaf inside the owner's `implement` blocks, the owner the `TypeRef` in hand |
| `compiler/sema/method_binding.cryo:1293` | `GenericRegistry::find_inherent_impl_generic_method` | ` ...` | J | member: a generic method's leaf inside the owner's `implement` blocks, the owner the `TypeRef` in hand |
| `compiler/sema/method_binding.cryo:852` | `GenericRegistry::find_inherent_impl_method` | ` ...` | J | member: a method's leaf inside the owner's `implement` blocks, the owner the `TypeRef` in hand |
| `compiler/sema/method_binding.cryo:751` | `GenericRegistry::find_trait_defining_method` | `method_name` | J | hint: the did-you-mean asks which trait declares a method of this leaf; the spelling is the question |
| `compiler/types/ownership.cryo:826` | `GenericRegistry::inherent_impl_has_method` | `ty, drop_sym` | J | member: a method's leaf inside the owner's `implement` blocks, the owner the `TypeRef` in hand |
| `compiler/mono/call_specializer.cryo:2359` | `GenericRegistry::template_in` | `ResBase::Def(scope_def), name` | J | member: a template declared inside its container - a module's generic function, a type's generic static method - by the leaf written after the path segment; the container is the segment's resolved identity (`ResBase`), never a spelling |
| `compiler/sema/async_lower.cryo:2655` | `GenericRegistry::template_in` | `ResBase::Def(owner), sr.member_name` | J | member: a template declared inside its container - a module's generic function, a type's generic static method - by the leaf written after the path segment; the container is the segment's resolved identity (`ResBase`), never a spelling |
| `compiler/sema/call_resolver.cryo:2572` | `GenericRegistry::template_in` | ` ...` | J | member: a template declared inside its container - a module's generic function, a type's generic static method - by the leaf written after the path segment; the container is the segment's resolved identity (`ResBase`), never a spelling |
| `compiler/sema/call_resolver.cryo:3672` | `GenericRegistry::template_in` | `ResBase::Def(owner_def), member_sym` | J | member: a template declared inside its container - a module's generic function, a type's generic static method - by the leaf written after the path segment; the container is the segment's resolved identity (`ResBase`), never a spelling |
| `compiler/sema/call_resolver.cryo:4358` | `GenericRegistry::template_in` | `b, scope.member_name` | J | member: a template declared inside its container - a module's generic function, a type's generic static method - by the leaf written after the path segment; the container is the segment's resolved identity (`ResBase`), never a spelling |
| `compiler/sema/call_resolver.cryo:5307` | `GenericRegistry::template_in` | ` ...` | J | member: a template declared inside its container - a module's generic function, a type's generic static method - by the leaf written after the path segment; the container is the segment's resolved identity (`ResBase`), never a spelling |
| `compiler/passes/type_resolution.cryo:1838` | `ImplBlockNode::generic_params[]` | `name` | J | member: the impl's own generic parameter by its leaf off the impl node in hand, scanned inline |
| `compiler/passes/type_resolution.cryo:210` | `ImplBlockNode::lookup_assoc_binding` | `adecl.name` | J | member: the impl's own `This::Member` binding by the member's leaf, asked from inside the block |
| `compiler/passes/type_resolution.cryo:287` | `ImplBlockNode::lookup_assoc_binding` | `adecl.name` | J | member: the impl's own `This::Member` binding by the member's leaf, asked from inside the block |
| `compiler/sema/async_lower.cryo:746` | `ImplBlockNode::lookup_assoc_binding` | `assoc` | J | member: the impl's own `This::Member` binding by the member's leaf, asked from inside the block |
| `compiler/mono/call_specializer.cryo:1235` | `ImplBlockNode::methods[]` | `ma.member` | J | member: the impl's own method by its leaf off the impl node in hand, scanned inline |
| `compiler/mono/call_specializer.cryo:1797` | `ImplBlockNode::methods[]` | `method_name` | J | member: the impl's own method by its leaf off the impl node in hand, scanned inline |
| `compiler/mono/call_specializer.cryo:1905` | `ImplBlockNode::methods[]` | `method_name` | J | member: the impl's own method by its leaf off the impl node in hand, scanned inline |
| `compiler/passes/type_resolution.cryo:1679` | `ImplBlockNode::methods[]` | `tfunc.name` | J | member: the impl's own method by its leaf off the impl node in hand, scanned inline |
| `compiler/sema/call_resolver.cryo:2962` | `ImplBlockNode::methods[]` | `member.member` | J | member: the impl's own method by its leaf off the impl node in hand, scanned inline |
| `compiler/sema/method_binding.cryo:952` | `ImplBlockNode::methods[]` | `method_name` | J | member: the impl's own method by its leaf off the impl node in hand, scanned inline |
| `compiler/sema/method_binding.cryo:1758` | `ImplBlockNode::methods[]` | `method_name` | J | member: the impl's own method by its leaf off the impl node in hand, scanned inline |
| `compiler/types/generic_registry.cryo:549` | `ImplBlockNode::methods[]` | `method_sym` | J | member: the impl's own method by its leaf off the impl node in hand, scanned inline |
| `compiler/types/generic_registry.cryo:572` | `ImplBlockNode::methods[]` | `method_sym` | J | member: the impl's own method by its leaf off the impl node in hand, scanned inline |
| `compiler/types/generic_registry.cryo:598` | `ImplBlockNode::methods[]` | `method_sym` | J | member: the impl's own method by its leaf off the impl node in hand, scanned inline |
| `compiler/types/ownership.cryo:1052` | `ImplBlockNode::methods[]` | `method_name` | J | member: the impl's own method by its leaf off the impl node in hand, scanned inline |
| `compiler/types/trait_checker.cryo:347` | `ImplBlockNode::methods[]` | `method_name` | J | member: the impl's own method by its leaf off the impl node in hand, scanned inline |
| `compiler/instance.cryo:895` | `ModuleGraph::find_module_index` | ` ...` | J | module: a module by its namespace path |
| `compiler/instance.cryo:3037` | `ModuleGraph::find_module_index` | `ctx.module_graph.module_named(qual)` | J | module: a module by its namespace path |
| `compiler/module_loader.cryo:645` | `ModuleGraph::find_module_index` | `graph.module_named(already_name)` | J | module: a module by its namespace path |
| `compiler/module_loader.cryo:776` | `ModuleGraph::find_module_index` | `graph.module_named(item_sym)` | J | module: a module by its namespace path |
| `compiler/module_loader.cryo:835` | `ModuleGraph::find_module_index` | `graph.module_named(exp_sym)` | J | module: a module by its namespace path |
| `compiler/module_loader.cryo:892` | `ModuleGraph::find_module_index` | `graph.module_named(eitem_sym)` | J | module: a module by its namespace path |
| `compiler/passes/pass_registry.cryo:1263` | `ModuleGraph::find_module_index` | `graph.module_named(mod_sym)` | J | module: a module by its namespace path |
| `compiler/resolver/name_resolution.cryo:579` | `ModuleGraph::find_module_index` | `this.ctx.module_graph.module_named(canonical)` | J | module: a module by its namespace path |
| `compiler/resolver/name_resolution.cryo:3187` | `ModuleGraph::find_module_index` | `this.ctx.module_graph.module_named(module_sym)` | J | module: a module by its namespace path |
| `compiler/resolver/name_resolution.cryo:3337` | `ModuleGraph::find_module_index` | `this.ctx.module_graph.module_named(sub_sym)` | J | module: a module by its namespace path |
| `compiler/compilation_context.cryo:656` | `ModuleGraph::module_def` | `this.module_graph.module_named(ns)` | J | module: a module's definition by its namespace path - the id the graph registered when it added the module, or the name layer's for a C import's module; a declaration asks it for its parent |
| `compiler/compilation_context.cryo:684` | `ModuleGraph::module_def` | `home` | J | module: a module's definition by its namespace path - the id the graph registered when it added the module, or the name layer's for a C import's module; a declaration asks it for its parent |
| `compiler/resolver/resolver.cryo:251` | `ModuleGraph::module_def` | `this.graph.module_named(this.intern_table.intern(sym.source_module))` | J | module: a module's definition by its namespace path - the id the graph registered when it added the module, or the name layer's for a C import's module; a declaration asks it for its parent |
| `compiler/sema/call_resolver.cryo:2528` | `ModuleGraph::module_def` | `this.ctx.module_graph.module_named(use_ns)` | J | module: a module's definition by its namespace path - the id the graph registered when it added the module, or the name layer's for a C import's module; a declaration asks it for its parent |
| `compiler/compilation_context.cryo:521` | `ModuleGraph::module_named` | `use_ns` | J | module: the one door from a namespace's text - an import's path, an export's item, a namespace another store recorded - to a module; it answers only a module the graph registered, so it can name a real module and never forge one |
| `compiler/compilation_context.cryo:656` | `ModuleGraph::module_named` | `ns` | J | module: the one door from a namespace's text - an import's path, an export's item, a namespace another store recorded - to a module; it answers only a module the graph registered, so it can name a real module and never forge one |
| `compiler/instance.cryo:896` | `ModuleGraph::module_named` | `ctx.intern_table.intern(ns)` | J | module: the one door from a namespace's text - an import's path, an export's item, a namespace another store recorded - to a module; it answers only a module the graph registered, so it can name a real module and never forge one |
| `compiler/instance.cryo:3037` | `ModuleGraph::module_named` | `qual` | J | module: the one door from a namespace's text - an import's path, an export's item, a namespace another store recorded - to a module; it answers only a module the graph registered, so it can name a real module and never forge one |
| `compiler/module_loader.cryo:645` | `ModuleGraph::module_named` | `already_name` | J | module: the one door from a namespace's text - an import's path, an export's item, a namespace another store recorded - to a module; it answers only a module the graph registered, so it can name a real module and never forge one |
| `compiler/module_loader.cryo:776` | `ModuleGraph::module_named` | `item_sym` | J | module: the one door from a namespace's text - an import's path, an export's item, a namespace another store recorded - to a module; it answers only a module the graph registered, so it can name a real module and never forge one |
| `compiler/module_loader.cryo:835` | `ModuleGraph::module_named` | `exp_sym` | J | module: the one door from a namespace's text - an import's path, an export's item, a namespace another store recorded - to a module; it answers only a module the graph registered, so it can name a real module and never forge one |
| `compiler/module_loader.cryo:892` | `ModuleGraph::module_named` | `eitem_sym` | J | module: the one door from a namespace's text - an import's path, an export's item, a namespace another store recorded - to a module; it answers only a module the graph registered, so it can name a real module and never forge one |
| `compiler/mono/monomorphizer.cryo:608` | `ModuleGraph::module_named` | `entry.module_name` | J | module: the one door from a namespace's text - an import's path, an export's item, a namespace another store recorded - to a module; it answers only a module the graph registered, so it can name a real module and never forge one |
| `compiler/passes/pass_registry.cryo:1263` | `ModuleGraph::module_named` | `mod_sym` | J | module: the one door from a namespace's text - an import's path, an export's item, a namespace another store recorded - to a module; it answers only a module the graph registered, so it can name a real module and never forge one |
| `compiler/resolver/name_resolution.cryo:579` | `ModuleGraph::module_named` | `canonical` | J | module: the one door from a namespace's text - an import's path, an export's item, a namespace another store recorded - to a module; it answers only a module the graph registered, so it can name a real module and never forge one |
| `compiler/resolver/name_resolution.cryo:2291` | `ModuleGraph::module_named` | `q_ns` | J | module: the one door from a namespace's text - an import's path, an export's item, a namespace another store recorded - to a module; it answers only a module the graph registered, so it can name a real module and never forge one |
| `compiler/resolver/name_resolution.cryo:2956` | `ModuleGraph::module_named` | `m` | J | module: the one door from a namespace's text - an import's path, an export's item, a namespace another store recorded - to a module; it answers only a module the graph registered, so it can name a real module and never forge one |
| `compiler/resolver/name_resolution.cryo:3004` | `ModuleGraph::module_named` | `m` | J | module: the one door from a namespace's text - an import's path, an export's item, a namespace another store recorded - to a module; it answers only a module the graph registered, so it can name a real module and never forge one |
| `compiler/resolver/name_resolution.cryo:3074` | `ModuleGraph::module_named` | `module_sym` | J | module: the one door from a namespace's text - an import's path, an export's item, a namespace another store recorded - to a module; it answers only a module the graph registered, so it can name a real module and never forge one |
| `compiler/resolver/name_resolution.cryo:3187` | `ModuleGraph::module_named` | `module_sym` | J | module: the one door from a namespace's text - an import's path, an export's item, a namespace another store recorded - to a module; it answers only a module the graph registered, so it can name a real module and never forge one |
| `compiler/resolver/name_resolution.cryo:3233` | `ModuleGraph::module_named` | `module_sym` | J | module: the one door from a namespace's text - an import's path, an export's item, a namespace another store recorded - to a module; it answers only a module the graph registered, so it can name a real module and never forge one |
| `compiler/resolver/name_resolution.cryo:3337` | `ModuleGraph::module_named` | `sub_sym` | J | module: the one door from a namespace's text - an import's path, an export's item, a namespace another store recorded - to a module; it answers only a module the graph registered, so it can name a real module and never forge one |
| `compiler/resolver/resolver.cryo:251` | `ModuleGraph::module_named` | `this.intern_table.intern(sym.source_module)` | J | module: the one door from a namespace's text - an import's path, an export's item, a namespace another store recorded - to a module; it answers only a module the graph registered, so it can name a real module and never forge one |
| `compiler/resolver/resolver.cryo:617` | `ModuleGraph::module_named` | `module_name` | J | module: the one door from a namespace's text - an import's path, an export's item, a namespace another store recorded - to a module; it answers only a module the graph registered, so it can name a real module and never forge one |
| `compiler/sema/call_resolver.cryo:2528` | `ModuleGraph::module_named` | `use_ns` | J | module: the one door from a namespace's text - an import's path, an export's item, a namespace another store recorded - to a module; it answers only a module the graph registered, so it can name a real module and never forge one |
| `compiler/sema/member_resolver.cryo:659` | `ModuleGraph::module_named` | `(t as StructType*).module_name` | J | module: the one door from a namespace's text - an import's path, an export's item, a namespace another store recorded - to a module; it answers only a module the graph registered, so it can name a real module and never forge one |
| `compiler/sema/member_resolver.cryo:662` | `ModuleGraph::module_named` | `(t as ClassType*).module_name` | J | module: the one door from a namespace's text - an import's path, an export's item, a namespace another store recorded - to a module; it answers only a module the graph registered, so it can name a real module and never forge one |
| `compiler/sema/sema.cryo:531` | `ModuleGraph::module_named` | `template_mod` | J | module: the one door from a namespace's text - an import's path, an export's item, a namespace another store recorded - to a module; it answers only a module the graph registered, so it can name a real module and never forge one |
| `compiler/instance.cryo:1416` | `ModuleGraph::modules[]` | `prelude_ns_sym.id` | J | module: the graph's modules by namespace, scanned inline |
| `compiler/resolver/name_resolution.cryo:1409` | `ModuleGraph::reexport_closure` | `cand.path()` | J | module: a module's re-export closure by its namespace path |
| `compiler/resolver/name_resolution.cryo:2956` | `ModuleGraph::reexport_closure` | `this.ctx.module_graph.module_named(m)` | J | module: a module's re-export closure by its namespace path |
| `compiler/resolver/name_resolution.cryo:3004` | `ModuleGraph::reexport_closure` | `this.ctx.module_graph.module_named(m)` | J | module: a module's re-export closure by its namespace path |
| `compiler/resolver/name_resolution.cryo:3074` | `ModuleGraph::reexport_closure` | `this.ctx.module_graph.module_named(module_sym)` | J | module: a module's re-export closure by its namespace path |
| `compiler/resolver/name_resolution.cryo:3233` | `ModuleGraph::reexport_closure` | `this.ctx.module_graph.module_named(module_sym)` | J | module: a module's re-export closure by its namespace path |
| `compiler/instance.cryo:1844` | `ModuleGraph::source_file_for_owner_key` | ` ...` | J | module: a module's source file by its namespace path |
| `compiler/passes/specialization.cryo:528` | `ModuleGraph::source_file_for_owner_key` | ` ...` | J | module: a module's source file by its namespace path |
| `compiler/passes/specialization.cryo:604` | `ModuleGraph::source_file_for_owner_key` | ` ...` | J | module: a module's source file by its namespace path |
| `compiler/instance.cryo:1468` | `ModuleInfo::reexports[]` | `imi_info.namespace_name.id` | J | module: a module's re-exported namespaces, module identities by path, scanned inline |
| `compiler/mono/monomorphizer.cryo:607` | `Resolver::find_module_scope` | ` ...` | J | module: a module's scope by its namespace path, to stand in it while a template is instantiated |
| `compiler/sema/sema.cryo:531` | `Resolver::find_module_scope` | `this.ctx.module_graph.module_named(template_mod)` | J | module: a module's scope by its namespace path, to stand in it while a template is instantiated |
| `compiler/AST/node_locator.cryo:1825` | `StructDeclNode::fields[]` | `field_name` | J | member: a struct declaration's field by its leaf off the node in hand, scanned inline |
| `compiler/sema/member_resolver.cryo:398` | `StructDeclNode::fields[]` | `field` | J | member: a struct declaration's field by its leaf off the node in hand, scanned inline |
| `compiler/sema/symbolic_checker.cryo:245` | `StructDeclNode::fields[]` | `field` | J | member: a struct declaration's field by its leaf off the node in hand, scanned inline |
| `compiler/types/ownership.cryo:1011` | `StructDeclNode::methods[]` | `method_name` | J | member: a struct declaration's method by its leaf off the node in hand, scanned inline |
| `compiler/sema/sema.cryo:3094` | `StructLiteralNode::field_inits[]` | `decl_name.id` | J | member: a struct literal's written initializer by the field's leaf, scanned inline |
| `compiler/sema/member_resolver.cryo:446` | `StructType::field_index` | `field_name` | J | member: a struct's field position by its leaf off the `StructType` in hand |
| `compiler/types/checker.cryo:1073` | `StructType::field_index` | `field_name` | J | member: a struct's field position by its leaf off the `StructType` in hand |
| `compiler/sema/diagnostics.cryo:308` | `StructType::get_method` | `member_name` | J | member: a struct's method by its leaf off the `StructType` in hand; Rust's method lookup is name-keyed off the owner too |
| `compiler/sema/lambda_synth.cryo:517` | `StructType::get_method` | `this.intern.intern("__call__")` | J | lang: the call protocol's `__call__`, the method leaf the language fixes for a callable struct |
| `compiler/sema/member_resolver.cryo:709` | `StructType::get_method` | `method_name` | J | member: a struct's method by its leaf off the `StructType` in hand; Rust's method lookup is name-keyed off the owner too |
| `compiler/sema/sema.cryo:3191` | `StructType::get_method` | `scope.member_name` | J | member: a struct's method by its leaf off the `StructType` in hand; Rust's method lookup is name-keyed off the owner too |
| `compiler/sema/sema.cryo:3211` | `StructType::get_method` | `scope.member_name` | J | member: a struct's method by its leaf off the `StructType` in hand; Rust's method lookup is name-keyed off the owner too |
| `compiler/types/checker.cryo:351` | `StructType::get_method` | ` ...` | J | lang: the call protocol's `__call__` (the argument continues on the next line), asked when a struct converts to a function type |
| `compiler/sema/call_resolver.cryo:455` | `StructType::methods[]` | `member.member` | J | member: a struct's method by its leaf off the `StructType` in hand, scanned inline with an arity predicate |
| `compiler/passes/type_resolution.cryo:1803` | `TraitDeclNode::assoc_type_index` | `member` | J | member: the trait's own associated type's position by its leaf, asked from the trait node in hand - the index the positional trait arguments are read by |
| `compiler/sema/method_binding.cryo:696` | `TraitDeclNode::assoc_type_index` | `member` | J | member: the trait's own associated type's position by its leaf, asked from the trait node in hand - the index the positional trait arguments are read by |
| `compiler/types/resolver.cryo:530` | `TraitDeclNode::assoc_type_index` | `member` | J | member: the trait's own associated type's position by its leaf, asked from the trait node in hand - the index the positional trait arguments are read by |
| `compiler/types/resolver.cryo:602` | `TraitDeclNode::assoc_type_index` | `member` | J | member: the trait's own associated type's position by its leaf, asked from the trait node in hand - the index the positional trait arguments are read by |
| `compiler/passes/type_resolution.cryo:1722` | `TraitDeclNode::lookup_assoc_type` | `assoc` | J | member: the trait's own associated type by its leaf, asked from the trait node in hand |
| `compiler/AST/node_locator.cryo:368` | `TraitDeclNode::lookup_method` | `method_name` | J | member: the trait's own method by its leaf, asked from the trait node in hand |
| `compiler/types/generic_registry.cryo:1294` | `TraitDeclNode::lookup_method` | `method_name` | J | member: the trait's own method by its leaf, asked from the trait node in hand |
| `compiler/passes/type_resolution.cryo:1674` | `TraitDeclNode::methods[]` | `assoc` | J | member: the trait's own method by its leaf off the trait node in hand, scanned inline (an `async` or a default-body predicate beside the leaf) |
| `compiler/sema/async_lower.cryo:743` | `TraitDeclNode::methods[]` | `node.name` | J | member: the trait's own method by its leaf off the trait node in hand, scanned inline (an `async` or a default-body predicate beside the leaf) |
| `compiler/sema/async_lower.cryo:2589` | `TraitDeclNode::methods[]` | `mname` | J | member: the trait's own method by its leaf off the trait node in hand, scanned inline (an `async` or a default-body predicate beside the leaf) |
| `compiler/sema/async_lower.cryo:2590` | `TraitDeclNode::methods[]` | `ap.member` | J | member: the trait's own method by its leaf off the trait node in hand, scanned inline (an `async` or a default-body predicate beside the leaf) |
| `compiler/sema/method_binding.cryo:668` | `TraitDeclNode::methods[]` | `method_name` | J | member: the trait's own method by its leaf off the trait node in hand, scanned inline (an `async` or a default-body predicate beside the leaf) |
| `compiler/sema/method_binding.cryo:986` | `TraitDeclNode::methods[]` | `method_name` | J | member: the trait's own method by its leaf off the trait node in hand, scanned inline (an `async` or a default-body predicate beside the leaf) |
| `compiler/sema/method_binding.cryo:1083` | `TraitDeclNode::methods[]` | `member.member` | J | member: the trait's own method by its leaf off the trait node in hand, scanned inline (an `async` or a default-body predicate beside the leaf) |
| `compiler/sema/method_binding.cryo:403` | `TraitType::required_methods[]` | `method_name` | J | member: a trait's required method by its leaf off the `TraitType` in hand, scanned inline |
| `compiler/sema/call_resolver.cryo:313` | `TypeUtils::lookup_func_type_exact` | `owner, sr.member_name` | J | member: the funnel's door onto `DeclarationIndex::lookup_func_type`, an overload family by its owner and leaf |
| `compiler/sema/call_resolver.cryo:2613` | `TypeUtils::lookup_func_type_exact` | `owner, leaf` | J | member: the funnel's door onto `DeclarationIndex::lookup_func_type`, an overload family by its owner and leaf |
| `compiler/sema/call_resolver.cryo:4280` | `TypeUtils::lookup_method_return` | `owner_ref, scope.member_name` | J | member: a method's return by its leaf off the owner `TypeRef` in hand (`method_owner_ref` for a wrapper; `lookup_type_exact` where the caller holds a stamp's name) |
| `compiler/sema/call_resolver.cryo:4310` | `TypeUtils::lookup_method_return` | `base_ty, scope.member_name` | J | member: a method's return by its leaf off the owner `TypeRef` in hand (`method_owner_ref` for a wrapper; `lookup_type_exact` where the caller holds a stamp's name) |
| `compiler/sema/call_resolver.cryo:5101` | `TypeUtils::lookup_method_return` | `type_ref, scope.member_name` | J | member: a method's return by its leaf off the owner `TypeRef` in hand (`method_owner_ref` for a wrapper; `lookup_type_exact` where the caller holds a stamp's name) |
| `compiler/sema/call_resolver.cryo:5607` | `TypeUtils::lookup_method_return` | `exp_ref, member_name` | J | member: a method's return by its leaf off the owner `TypeRef` in hand (`method_owner_ref` for a wrapper; `lookup_type_exact` where the caller holds a stamp's name) |
| `compiler/sema/method_binding.cryo:174` | `TypeUtils::lookup_method_return` | `base_ref, method_name` | J | member: a method's return by its leaf off the owner `TypeRef` in hand (`method_owner_ref` for a wrapper; `lookup_type_exact` where the caller holds a stamp's name) |
| `compiler/sema/method_binding.cryo:342` | `TypeUtils::lookup_method_return` | ` ...` | J | member: a method's return by its leaf off the owner `TypeRef` in hand (`method_owner_ref` for a wrapper; `lookup_type_exact` where the caller holds a stamp's name) |
| `compiler/sema/method_binding.cryo:432` | `TypeUtils::lookup_method_return` | ` ...` | J | member: a method's return by its leaf off the owner `TypeRef` in hand (`method_owner_ref` for a wrapper; `lookup_type_exact` where the caller holds a stamp's name) |
| `compiler/sema/method_binding.cryo:740` | `TypeUtils::lookup_method_return` | ` ...` | J | member: a method's return by its leaf off the owner `TypeRef` in hand (`method_owner_ref` for a wrapper; `lookup_type_exact` where the caller holds a stamp's name) |
| `compiler/sema/method_binding.cryo:758` | `TypeUtils::lookup_method_return` | ` ...` | J | member: a method's return by its leaf off the owner `TypeRef` in hand (`method_owner_ref` for a wrapper; `lookup_type_exact` where the caller holds a stamp's name) |
| `compiler/sema/method_binding.cryo:771` | `TypeUtils::lookup_method_return` | `parent_ref, method_name` | J | member: a method's return by its leaf off the owner `TypeRef` in hand (`method_owner_ref` for a wrapper; `lookup_type_exact` where the caller holds a stamp's name) |
| `compiler/sema/sema.cryo:1262` | `TypeUtils::lookup_method_return` | `owner_ref, this.intern.intern("iter")` | J | member: the for-in protocol's `iter` off a collection that does not implement `Iterator` |
| `compiler/AST/node_locator.cryo:1834` | `UnionDeclNode::fields[]` | `field_name` | J | member: a union declaration's field by its leaf off the node in hand, scanned inline |
| `compiler/sema/member_resolver.cryo:408` | `UnionDeclNode::fields[]` | `field` | J | member: a union declaration's field by its leaf off the node in hand, scanned inline |
| `compiler/sema/symbolic_checker.cryo:254` | `UnionDeclNode::fields[]` | `field` | J | member: a union declaration's field by its leaf off the node in hand, scanned inline |
| `compiler/types/ownership.cryo:1021` | `UnionDeclNode::methods[]` | `method_name` | J | member: a union declaration's method by its leaf off the node in hand, scanned inline |
| `compiler/passes/ast_validation.cryo:213` | `local::FieldDeclNode[]` | `seen[k]` | J | member: a declaration's written fields held in a local, by leaf (the duplicate-field check) |
| `compiler/codegen/visit/ir_generator.cryo:909` | `local::FieldInfo[]` | `b.source_field.id` | J | member: a struct's or class's fields held by reference in a local, by leaf |
| `compiler/passes/drop_insertion.cryo:3653` | `local::FieldInfo[]` | `key` | J | member: a struct's or class's fields held by reference in a local, by leaf |
| `compiler/sema/sema.cryo:1441` | `local::FieldInfo[]` | `b.source_field.id` | J | member: a struct's or class's fields held by reference in a local, by leaf |
| `compiler/parser/parser.cryo:2336` | `local::GenericParamNode[]` | `gp.name` | J | member: a declaration's own written generic parameters, scanned for a repeated spelling as the parser builds the list. The question IS about spellings - two parameters written `<T, T>` are a redeclaration - and at parse time there is no identity to ask instead: the resolver stamps a `SymbolID` on each parameter only after the list exists. Rust refuses the same shape the same way, by comparing idents in the parameter list |
| `compiler/sema/call_resolver.cryo:2312` | `local::MethodInfo[]` | `member.member` | J | member: an arena type's methods held in a local, by leaf |
| `compiler/sema/call_resolver.cryo:5824` | `local::MethodInfo[]` | `scope.member_name` | J | member: an arena type's methods held in a local, by leaf |
| `compiler/sema/call_resolver.cryo:5843` | `local::MethodInfo[]` | `scope.member_name` | J | member: an arena type's methods held in a local, by leaf |
| `compiler/AST/node_locator.cryo:1275` | `local::MethodNode[]` | `name` | J | member: an impl's or a declaration's methods held in a local, by leaf |
| `compiler/mono/call_specializer.cryo:1305` | `local::MethodNode[]` | `scope.member_name` | J | member: an impl's or a declaration's methods held in a local, by leaf |
| `compiler/mono/call_specializer.cryo:1601` | `local::MethodNode[]` | `method_name` | J | member: an impl's or a declaration's methods held in a local, by leaf |
| `compiler/mono/call_specializer.cryo:1628` | `local::MethodNode[]` | `method_name` | J | member: an impl's or a declaration's methods held in a local, by leaf |
| `compiler/mono/call_specializer.cryo:1832` | `local::MethodNode[]` | `method_name` | J | member: an impl's or a declaration's methods held in a local, by leaf |
| `compiler/sema/call_resolver.cryo:4518` | `local::MethodNode[]` | `scope.member_name` | J | member: an impl's or a declaration's methods held in a local, by leaf |
| `compiler/sema/call_resolver.cryo:4699` | `local::MethodNode[]` | `scope.member_name` | J | member: an impl's or a declaration's methods held in a local, by leaf |
| `compiler/sema/call_resolver.cryo:4729` | `local::MethodNode[]` | `scope.member_name` | J | member: an impl's or a declaration's methods held in a local, by leaf |
| `compiler/sema/method_binding.cryo:163` | `local::MethodNode[]` | `method_name` | J | member: an impl's or a declaration's methods held in a local, by leaf |
| `compiler/sema/method_binding.cryo:895` | `local::MethodNode[]` | `member.member` | J | member: an impl's or a declaration's methods held in a local, by leaf |
| `compiler/sema/method_binding.cryo:966` | `local::MethodNode[]` | `name` | J | member: an impl's or a declaration's methods held in a local, by leaf |
| `compiler/sema/symbolic_checker.cryo:317` | `local::MethodNode[]` | `method_name` | J | member: an impl's or a declaration's methods held in a local, by leaf |
| `compiler/codegen/ops/declaration_emitter.cryo:745` | `local::VTableSlot[]` | `m.name` | J | member: a class's vtable slots held in a local, by the method's leaf (the override walk) |
| `compiler/codegen/ops/declaration_emitter.cryo:845` | `local::VTableSlot[]` | `method_name` | J | member: a class's vtable slots held in a local, by the method's leaf (the override walk) |
| `compiler/resolver/name_resolution.cryo:2910` | `ConstantTable::intern_qualified` | `intern, ns, node.name` | N | a key MINTED at the constant's declaration (`ns::name`) for its registration; the write side |
| `compiler/resolver/name_resolution.cryo:2940` | `ConstantTable::intern_qualified` | `intern, ns, node.name` | N | a key MINTED at the constant's declaration (`ns::name`) for its registration; the write side |
| `compiler/passes/drop_insertion.cryo:3552` | `ConstantTable::parse_int_literal` | `text, &v` | N | a literal's text parsed to a value |
| `compiler/codegen/ops/declaration_emitter.cryo:642` | `DeclarationIndex::impl_owner` | `node, "codegen/declare impl"` | N | keyed by the impl node; the string is the diagnostic label of the asking site |
| `compiler/codegen/visit/decl_visit_emitter.cryo:216` | `DeclarationIndex::impl_owner` | `node, "codegen/impl bodies"` | N | keyed by the impl node; the string is the diagnostic label of the asking site |
| `compiler/passes/specialization.cryo:973` | `DeclarationIndex::impl_owner` | `node, "specialization/inherent impl"` | N | keyed by the impl node; the string is the diagnostic label of the asking site |
| `compiler/passes/specialization.cryo:998` | `DeclarationIndex::impl_owner` | `node, "specialization/trait impl"` | N | keyed by the impl node; the string is the diagnostic label of the asking site |
| `compiler/passes/type_resolution.cryo:1138` | `DeclarationIndex::impl_owner` | `node, "type_resolution/trait impl"` | N | keyed by the impl node; the string is the diagnostic label of the asking site |
| `compiler/passes/type_resolution.cryo:1194` | `DeclarationIndex::impl_owner` | `node, "type_resolution/overlap"` | N | keyed by the impl node; the string is the diagnostic label of the asking site |
| `compiler/passes/type_resolution.cryo:1223` | `DeclarationIndex::impl_owner` | `node, "type_resolution/register impl methods"` | N | keyed by the impl node; the string is the diagnostic label of the asking site |
| `compiler/passes/type_resolution.cryo:2411` | `DeclarationIndex::impl_owner` | `node, "type_resolution/impl methods"` | N | keyed by the impl node; the string is the diagnostic label of the asking site |
| `compiler/passes/type_resolution.cryo:2753` | `DeclarationIndex::impl_owner` | `node, "type_resolution/impl methods"` | N | keyed by the impl node; the string is the diagnostic label of the asking site |
| `compiler/passes/type_resolution.cryo:3462` | `DeclarationIndex::impl_owner` | `node, "type_resolution/struct field sync"` | N | keyed by the impl node; the string is the diagnostic label of the asking site |
| `compiler/sema/sema.cryo:392` | `DeclarationIndex::impl_owner` | `ib, "sema/async declare"` | N | keyed by the impl node; the string is the diagnostic label of the asking site |
| `compiler/sema/sema.cryo:642` | `DeclarationIndex::impl_owner` | `node, "sema/visit:ImplBlockNode"` | N | keyed by the impl node; the string is the diagnostic label of the asking site |
| `compiler/sema/sema.cryo:674` | `DeclarationIndex::impl_owner` | `node, "sema/visit:ImplBlockNode"` | N | keyed by the impl node; the string is the diagnostic label of the asking site |
| `compiler/sema/sema.cryo:695` | `DeclarationIndex::impl_owner` | `node, "sema/visit:ImplBlockNode"` | N | keyed by the impl node; the string is the diagnostic label of the asking site |
| `compiler/sema/type_utils.cryo:213` | `DeclarationIndex::lookup_func_type` | `owner, leaf` | N | the funnel's own forwarding body (`lookup_func_type_exact`); its callers are the sites |
| `compiler/sema/type_utils.cryo:166` | `DeclarationIndex::lookup_method_return` | `owner, method_sym` | N | the funnel's own forwarding body (`lookup_method_return`); its callers are the sites |
| `compiler/sema/type_utils.cryo:207` | `DeclarationIndex::lookup_type` | `name` | N | the funnel's own forwarding body (`lookup_type_exact`); its callers are the sites |
| `compiler/codegen/ops/declaration_emitter.cryo:235` | `DeclarationIndex::type_of_decl` | `node.def, "codegen/declare struct type"` | N | keyed by the `DefId`; the string is the diagnostic label of the asking site |
| `compiler/codegen/ops/declaration_emitter.cryo:251` | `DeclarationIndex::type_of_decl` | `node.def, "codegen/declare union type"` | N | keyed by the `DefId`; the string is the diagnostic label of the asking site |
| `compiler/codegen/ops/declaration_emitter.cryo:264` | `DeclarationIndex::type_of_decl` | `node.def, "codegen/declare class type"` | N | keyed by the `DefId`; the string is the diagnostic label of the asking site |
| `compiler/codegen/ops/declaration_emitter.cryo:278` | `DeclarationIndex::type_of_decl` | `node.def, "codegen/declare enum type"` | N | keyed by the `DefId`; the string is the diagnostic label of the asking site |
| `compiler/codegen/ops/declaration_emitter.cryo:656` | `DeclarationIndex::type_of_decl` | `node.def, "codegen/declare struct methods"` | N | keyed by the `DefId`; the string is the diagnostic label of the asking site |
| `compiler/codegen/ops/declaration_emitter.cryo:671` | `DeclarationIndex::type_of_decl` | `node.def, "codegen/declare union methods"` | N | keyed by the `DefId`; the string is the diagnostic label of the asking site |
| `compiler/codegen/ops/declaration_emitter.cryo:682` | `DeclarationIndex::type_of_decl` | `node.def, "codegen/declare class methods"` | N | keyed by the `DefId`; the string is the diagnostic label of the asking site |
| `compiler/codegen/visit/decl_visit_emitter.cryo:113` | `DeclarationIndex::type_of_decl` | `node.def, "codegen/struct method bodies"` | N | keyed by the `DefId`; the string is the diagnostic label of the asking site |
| `compiler/codegen/visit/decl_visit_emitter.cryo:127` | `DeclarationIndex::type_of_decl` | `node.def, "codegen/union method bodies"` | N | keyed by the `DefId`; the string is the diagnostic label of the asking site |
| `compiler/codegen/visit/decl_visit_emitter.cryo:141` | `DeclarationIndex::type_of_decl` | `node.def, "codegen/class method bodies"` | N | keyed by the `DefId`; the string is the diagnostic label of the asking site |
| `compiler/passes/directive_processing.cryo:1703` | `DeclarationIndex::type_of_decl` | `node.def, "directive/struct layout"` | N | keyed by the `DefId`; the string is the diagnostic label of the asking site |
| `compiler/passes/directive_processing.cryo:1720` | `DeclarationIndex::type_of_decl` | `node.def, "directive/union layout"` | N | keyed by the `DefId`; the string is the diagnostic label of the asking site |
| `compiler/passes/directive_processing.cryo:1735` | `DeclarationIndex::type_of_decl` | `node.def, "directive/class layout"` | N | keyed by the `DefId`; the string is the diagnostic label of the asking site |
| `compiler/passes/directive_processing.cryo:1747` | `DeclarationIndex::type_of_decl` | `node.def, "directive/enum layout"` | N | keyed by the `DefId`; the string is the diagnostic label of the asking site |
| `compiler/passes/specialization.cryo:173` | `DeclarationIndex::type_of_decl` | `node.def, "specialization/inherent owner"` | N | keyed by the `DefId`; the string is the diagnostic label of the asking site |
| `compiler/passes/specialization.cryo:190` | `DeclarationIndex::type_of_decl` | `node.def, "specialization/inherent owner"` | N | keyed by the `DefId`; the string is the diagnostic label of the asking site |
| `compiler/passes/specialization.cryo:211` | `DeclarationIndex::type_of_decl` | `node.def, "specialization/inherent owner"` | N | keyed by the `DefId`; the string is the diagnostic label of the asking site |
| `compiler/passes/type_resolution.cryo:978` | `DeclarationIndex::type_of_decl` | `node.def, "type_resolution/register struct methods"` | N | keyed by the `DefId`; the string is the diagnostic label of the asking site |
| `compiler/passes/type_resolution.cryo:982` | `DeclarationIndex::type_of_decl` | `node.def, "type_resolution/register union methods"` | N | keyed by the `DefId`; the string is the diagnostic label of the asking site |
| `compiler/passes/type_resolution.cryo:986` | `DeclarationIndex::type_of_decl` | `node.def, "type_resolution/register class methods"` | N | keyed by the `DefId`; the string is the diagnostic label of the asking site |
| `compiler/passes/type_resolution.cryo:2269` | `DeclarationIndex::type_of_decl` | `node.def, "type_resolution/struct methods"` | N | keyed by the `DefId`; the string is the diagnostic label of the asking site |
| `compiler/passes/type_resolution.cryo:2277` | `DeclarationIndex::type_of_decl` | `node.def, "type_resolution/union methods"` | N | keyed by the `DefId`; the string is the diagnostic label of the asking site |
| `compiler/passes/type_resolution.cryo:2285` | `DeclarationIndex::type_of_decl` | `node.def, "type_resolution/class methods"` | N | keyed by the `DefId`; the string is the diagnostic label of the asking site |
| `compiler/passes/type_resolution.cryo:2316` | `DeclarationIndex::type_of_decl` | `node.def, "type_resolution/trait populate"` | N | keyed by the `DefId`; the string is the diagnostic label of the asking site |
| `compiler/passes/type_resolution.cryo:2654` | `DeclarationIndex::type_of_decl` | `node.def, "type_resolution/alias target"` | N | keyed by the `DefId`; the string is the diagnostic label of the asking site |
| `compiler/passes/type_resolution.cryo:3097` | `DeclarationIndex::type_of_decl` | `node.def, "type_resolution/struct field sync"` | N | keyed by the `DefId`; the string is the diagnostic label of the asking site |
| `compiler/passes/type_resolution.cryo:3172` | `DeclarationIndex::type_of_decl` | `node.def, "type_resolution/union field sync"` | N | keyed by the `DefId`; the string is the diagnostic label of the asking site |
| `compiler/passes/type_resolution.cryo:3232` | `DeclarationIndex::type_of_decl` | `node.def, "type_resolution/class field sync"` | N | keyed by the `DefId`; the string is the diagnostic label of the asking site |
| `compiler/passes/type_resolution.cryo:3347` | `DeclarationIndex::type_of_decl` | `node.def, "type_resolution/enum field sync"` | N | keyed by the `DefId`; the string is the diagnostic label of the asking site |
| `compiler/sema/sema.cryo:374` | `DeclarationIndex::type_of_decl` | `s.def, "sema/async declare struct"` | N | keyed by the `DefId`; the string is the diagnostic label of the asking site |
| `compiler/sema/sema.cryo:379` | `DeclarationIndex::type_of_decl` | `u.def, "sema/async declare union"` | N | keyed by the `DefId`; the string is the diagnostic label of the asking site |
| `compiler/sema/sema.cryo:384` | `DeclarationIndex::type_of_decl` | `c.def, "sema/async declare class"` | N | keyed by the `DefId`; the string is the diagnostic label of the asking site |
| `compiler/sema/sema.cryo:591` | `DeclarationIndex::type_of_decl` | `node.def, "sema/visit:StructDeclNode"` | N | keyed by the `DefId`; the string is the diagnostic label of the asking site |
| `compiler/sema/sema.cryo:600` | `DeclarationIndex::type_of_decl` | `node.def, "sema/visit:UnionDeclNode"` | N | keyed by the `DefId`; the string is the diagnostic label of the asking site |
| `compiler/sema/sema.cryo:609` | `DeclarationIndex::type_of_decl` | `node.def, "sema/visit:ClassDeclNode"` | N | keyed by the `DefId`; the string is the diagnostic label of the asking site |
| `compiler/AST/dumper.cryo:789` | `DestructureDeclNode::bindings[]` | `b.local_name.id` | N | a display: the binding's own two names compared to print `x` rather than `x: x` |
| `compiler/passes/specialization.cryo:899` | `GenericRegistry::member_template_key` | ` ...` | N | a key MINTED at the member template's declaration (`Owner::method`) for its registration; the write side of `template_in` |
| `compiler/mono/specializer.cryo:234` | `ImplBlockNode::set_target_type` | `spec_sym` | N | a write: the specialized impl's target name stored on the node |
| `compiler/passes/specialization.cryo:689` | `ImplBlockNode::set_target_type` | `impl_qname` | N | a write: the specialized impl's target name stored on the node |
| `compiler/AST/node_locator.cryo:326` | `ModuleGraph::find_module_by_path` | `file_path` | N | a module by its FILE PATH |
| `compiler/AST/node_locator.cryo:465` | `ModuleGraph::find_module_by_path` | `file_path` | N | a module by its FILE PATH |
| `compiler/AST/node_locator.cryo:497` | `ModuleGraph::find_module_by_path` | `target.span.file` | N | a module by its FILE PATH |
| `compiler/AST/node_locator.cryo:1431` | `ModuleGraph::find_module_by_path` | `file` | N | a module by its FILE PATH |
| `compiler/codegen/passes.cryo:1408` | `ModuleGraph::find_module_by_path` | `ctx.source_file` | N | a module by its FILE PATH |
| `compiler/instance.cryo:1826` | `ModuleGraph::find_module_by_path` | ` ...` | N | a module by its FILE PATH |
| `compiler/instance.cryo:1847` | `ModuleGraph::find_module_by_path` | ` ...` | N | a module by its FILE PATH |
| `compiler/module_loader.cryo:701` | `ModuleGraph::find_module_by_path` | `resolved` | N | a module by its FILE PATH |
| `compiler/module_loader.cryo:756` | `ModuleGraph::find_module_by_path` | `item_resolved` | N | a module by its FILE PATH |
| `compiler/module_loader.cryo:814` | `ModuleGraph::find_module_by_path` | `exp_resolved` | N | a module by its FILE PATH |
| `compiler/module_loader.cryo:875` | `ModuleGraph::find_module_by_path` | `eitem_resolved` | N | a module by its FILE PATH |
| `compiler/module_loader.cryo:923` | `ModuleGraph::find_module_by_path` | `sub_resolved` | N | a module by its FILE PATH |
| `compiler/compilation_context.cryo:663` | `ModuleGraph::module_of_file` | `file` | N | the module a source FILE declares, by its FILE PATH |
| `compiler/resolver/name_resolution.cryo:1514` | `ModuleGraph::module_of_file` | `span.file` | N | the module a source FILE declares, by its FILE PATH |
| `compiler/resolver/name_resolution.cryo:2313` | `ModuleGraph::module_of_file` | `span.file` | N | the module a source FILE declares, by its FILE PATH |
| `compiler/compilation_context.cryo:490` | `ModuleGraph::ns_sym_of_file` | `file` | N | a namespace by its FILE PATH |
| `compiler/codegen/visit/lambda_emitter.cryo:127` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/codegen/visit/lambda_emitter.cryo:142` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/mono/ast_resolver.cryo:107` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/mono/ast_resolver.cryo:181` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/mono/call_specializer.cryo:164` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/mono/call_specializer.cryo:240` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/mono/call_specializer.cryo:1164` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/mono/call_specializer.cryo:1178` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/mono/call_specializer.cryo:1254` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/mono/call_specializer.cryo:1403` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/mono/call_specializer.cryo:1511` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/mono/call_specializer.cryo:1515` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/mono/call_specializer.cryo:1551` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/mono/call_specializer.cryo:1724` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/mono/call_specializer.cryo:2047` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/mono/monomorphizer.cryo:322` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/mono/state.cryo:347` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/mono/state.cryo:407` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/passes/type_resolution.cryo:117` | `ResolutionContext::new` | `ctx.source_file` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/passes/type_resolution.cryo:3369` | `ResolutionContext::new` | `ctx.source_file` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/sema/call_resolver.cryo:657` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/sema/call_resolver.cryo:1426` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/sema/call_resolver.cryo:1558` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/sema/call_resolver.cryo:1621` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/sema/call_resolver.cryo:1785` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/sema/call_resolver.cryo:4407` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/sema/call_resolver.cryo:4596` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/sema/call_resolver.cryo:4655` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/sema/call_resolver.cryo:4925` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/sema/call_resolver.cryo:4988` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/sema/call_resolver.cryo:5373` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/sema/call_resolver.cryo:5469` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/sema/call_resolver.cryo:5521` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/sema/call_resolver.cryo:5569` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/sema/lambda_synth.cryo:120` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/sema/method_binding.cryo:464` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/sema/method_binding.cryo:795` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/sema/method_binding.cryo:856` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/sema/method_binding.cryo:912` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/sema/method_binding.cryo:1028` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/sema/method_binding.cryo:1143` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/sema/method_binding.cryo:1165` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/sema/method_binding.cryo:1403` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/sema/method_binding.cryo:1769` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/sema/sema.cryo:773` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/sema/sema.cryo:888` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/sema/sema.cryo:1593` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/sema/sema.cryo:2392` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/sema/sema.cryo:2644` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/sema/sema.cryo:3926` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/sema/symbolic_checker.cryo:160` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/passes/type_resolution.cryo:2336` | `TraitType::add_assoc_type` | `adecl.name` | N | a write: the trait's own associated-type name recorded on the trait type |
<!-- residue-table:end -->

## Elsewhere: population names on types that hold nothing

Every dotted call to a population name whose receiver is placed on a type
that is NOT a holder, per (type, method). These are not sites - the type
holds no declaration - but they are pinned, because a holder the placement
rule misreads as another type lands here and nowhere else; this is the
lane gate's LOOKUP_LOCAL row, by name. A static call's owner is spelled and
cannot be misplaced, so statics on non-holders are not tallied. When a row
here moves, either a receiver was misplaced (place it) or a same-named
method on an unrelated type gained or lost a caller (regenerate, and say
which in the commit).

<!-- residue-elsewhere:begin -->
Elsewhere **20** - a population name on a type that holds nothing, per (type, method)

| receiver type and read | calls |
|---|---|
| `CompilationContext::module_of_file` | 3 |
| `DropInserter::lookup_type` | 4 |
| `ModuleKeyTable::lookup` | 1 |
| `MoveChecker::lookup_type` | 4 |
| `Scope::is_ambiguous` | 2 |
| `ScopeManager::lookup_local` | 5 |
| `TypeMapperCache::lookup` | 1 |
<!-- residue-elsewhere:end -->
