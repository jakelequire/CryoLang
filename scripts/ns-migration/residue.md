# D32's residue: every name-keyed read in `compiler/src`, classified

The completion criterion for merging `naming-impl` (`docs/name-resolution.md`
§0.1 D32, §8.276): **zero name-keyed reads outside an enumerated, justified
residue, pinned by count.** This file is the enumeration. It is one list;
every site carries its class and a one-line reason, and the J rows are the
residue Jake rules on.

## How the list is made, and how it is checked

* **The population is derived, not recalled.** `residue.py` builds it from
  `scripts/lane-gate.py`'s own parser - the stores rule 1 places, the array
  owners rule 1b places as *a member table inside its owner*, rule 2's
  signature test narrowed to a key type in PARAMETER position on a read
  (`&this` or static), and the receiver placed by its declared type (a
  dotted receiver by its annotation, a cast receiver by the type the cast
  names, a static by its spelled owner, turbofish included). A
  reader added under any spelling on any store is in the population the
  moment it is declared; a type excluded by the gate for holding paths,
  flags, texts or a pass's own rib (`DiagSink`, `ParsedArgs`,
  `ProjectConfig`, `AsyncLower`, ...) holds no declaration and is outside.
  `python scripts/ns-migration/residue.py --count` is the size;
  without a flag it prints every site as `file`, `line`, receiver type,
  method, argument text.
* **The classes are judgement, held as code.** `residue_classify.py` maps
  each read METHOD to a class and a reason, with a per-site override where
  one call's key comes from somewhere the method's other callers' do not,
  and writes the table below. A method it classifies that the tree no
  longer declares, a read method the tree calls that it does not classify,
  or an override matching no call, each refuse the run.
* **The check.** `python scripts/ns-migration/residue.py --check` matches
  the table to the tree by (file, method, key text) - a line shift is not
  drift, a site added, removed or re-keyed is - and re-derives every row's
  class from `residue_classify.py`, refusing a row whose letter is not the
  classifier's (flipped, blanked, or a letter the classes do not include);
  on OK it prints the count per class from the classifier, so the number
  §0 pins is checked against the tree, the list and the code that decides
  it. A check that read the letters as written let a J row flipped to N by
  hand pass with J one lower. It also refuses a call to a population name
  that no receiver pattern reaches (a parenthesized receiver, a call with
  arguments), and pins the *elsewhere* table below. `residue_selftest.py`
  (`make residue-selftest`, in `check-fast` and CI) drives each refusal
  through a throwaway tree and list. §0's D32 row carries the command and
  the counts.
* The table is regenerated by `python scripts/ns-migration/residue_classify.py`;
  a run that changes a row's class is a change of judgement and goes in
  the ledger with its reason.

## The classes

| class | meaning | in the residue? |
|---|---|---|
| **J** | **justified** - name-keyed by design; the sub-kind is the reason's first word: `member` (a member's leaf inside a settled owner - Rust's field and method lookup is name-keyed off the owner too), `lang` (a declaration the language names by its own spelling and claims at declaration - Rust's lang items are keyed by the attribute's string), `module` (a module by its namespace path - the module graph is the declaration of modules and a namespace names exactly one, D5), `prim` (a primitive by the spelling its `Res::PrimTy` stamp carries), `extern` (a C symbol by its link name), `hint` (a did-you-mean whose question is the spelling) | **yes - these are the count** |
| N | not a key - the string names no declaration: a diagnostic label beside a `DefId`, a file path, a literal's text, a constructor's source file, a write onto an AST node, a funnel's own forwarding body. In the population because the gate's parameter test cannot tell a label from a key | no - outside D32 |
| S | stamp-derived - an index or funnel door keyed by a canonical string the caller derived from a stamp or a `TypeRef`. Convertible: the door can take the stamp | no - convertible, remaining work |
| B | the `GenericRegistry` keyed by the canonical string of a stamped identity - bucket B's reader side (§8.245); re-keying the registry by identity is Jake's unit | no - convertible, Jake's unit |
| C | a key composed from parts (`owner::member`, a family string, `ns::name`) - bucket C, behind pin-as-declaration | no - convertible, blocked |
| F | visibility asked by the callee's name (`enforce_callee_visibility`) - bucket F, behind pin-as-declaration | no - convertible, blocked |
| W | a written spelling with no stamp behind it and no reason above | no - must convert |

**The migration is complete when S, B, C, F and W are all 0 and J is at or
below the ceiling Jake sets.** The J sub-kinds are separable: if `module`
(a module by its path) or `lang` (a language-named declaration by its
spelling) is ruled NOT by design, the rows are already grouped for it and
the ceiling moves down by that sub-kind's count; nothing here pads or
trims toward a number.

Two shapes the ledger named for this list before it existed, and where
they landed: the member table inside its owner (`ImplBlockNode::lookup_assoc_binding`,
`ResolutionContext::lookup_assoc_binding`, `TraitType::get_method`) is J
`member` - `get_method` has no caller outside its file, so it has no row;
`TemplateEntry.param_names`' spelling compares in `call_resolver.cryo`
are `equals` calls on an array, not a method of a holder, so they are not
in this population - they are §8.275's arena unit and retire with it.

A third shape the first list could not see: the member tables of the
user-defined types themselves. `StructType.fields: FieldInfo[]`,
`ClassType.methods: MethodInfo[]`, `EnumType.variants: EnumVariantInfo[]`
and `TraitDeclNode.assoc_types` are arrays of RECORDS with the name in
each, searched by `rows[i].name.equals(n)` - neither a map nor an array
of names, so no rule reached them and their 43 callers were nobody's.
The gate's rule 1b now takes an array of records carrying a key-typed
field as a candidate (37 owners on this tree, 20 placed as exclusions by
reason, four of them member tables), and those callers are J `member` -
a struct's field or method, an enum's variant, a trait's associated type,
each by its leaf off the type already in hand - or J `lang` where the leaf
is one the language fixes (`__call__`, `Ready`, `Ok`/`Err`, `Some`/`None`).

## The table

<!-- residue-table:begin -->
Population **306** - J 93 · N 117 · S 26 · B 49 · C 19 · F 2 · W 0

| site | read | key as written | class | reason |
|---|---|---|---|---|
| `compiler/mono/call_specializer.cryo:905` | `GenericRegistry::find_inherent_impl_generic_method` | ` ...` | B | `(owner, method)`: the owner a canonical name off a `TypeRef`, the method a leaf inside it |
| `compiler/sema/method_binding.cryo:1383` | `GenericRegistry::find_inherent_impl_generic_method` | ` ...` | B | `(owner, method)`: the owner a canonical name off a `TypeRef`, the method a leaf inside it |
| `compiler/sema/method_binding.cryo:1389` | `GenericRegistry::find_inherent_impl_generic_method` | ` ...` | B | `(owner, method)`: the owner a canonical name off a `TypeRef`, the method a leaf inside it |
| `compiler/sema/method_binding.cryo:927` | `GenericRegistry::find_inherent_impl_method` | ` ...` | B | `(owner, method)`: the owner a canonical name off a `TypeRef`, the method a leaf inside it |
| `compiler/codegen/ops/declaration_emitter.cryo:636` | `GenericRegistry::get_template` | `qualified_target` | B | a template by the canonical name of a stamped type (`target_key`, `get_qualified_name`, a `Def` stamp, the call's pinned template key) |
| `compiler/codegen/visit/decl_visit_emitter.cryo:212` | `GenericRegistry::get_template` | `qualified_impl_target` | B | a template by the canonical name of a stamped type (`target_key`, `get_qualified_name`, a `Def` stamp, the call's pinned template key) |
| `compiler/mono/call_specializer.cryo:569` | `GenericRegistry::get_template` | `call.resolved_template` | B | the call's pinned template KEY, a registry key string sema recorded |
| `compiler/passes/specialization.cryo:960` | `GenericRegistry::get_template` | `qualified_sym` | B | a template by the canonical name of a stamped type (`target_key`, `get_qualified_name`, a `Def` stamp, the call's pinned template key) |
| `compiler/passes/type_resolution.cryo:1520` | `GenericRegistry::get_template` | `canonical_target` | B | a template by the canonical name of a stamped type (`target_key`, `get_qualified_name`, a `Def` stamp, the call's pinned template key) |
| `compiler/sema/async_lower.cryo:2553` | `GenericRegistry::get_template` | `c.resolved_template` | B | the call's pinned template KEY, a registry key string sema recorded |
| `compiler/sema/async_lower.cryo:2568` | `GenericRegistry::get_template` | ` ...` | B | a template by the canonical name of a stamped type (`target_key`, `get_qualified_name`, a `Def` stamp, the call's pinned template key) |
| `compiler/sema/call_resolver.cryo:352` | `GenericRegistry::get_template` | `owner` | B | a template by the canonical name of a stamped type (`target_key`, `get_qualified_name`, a `Def` stamp, the call's pinned template key) |
| `compiler/sema/call_resolver.cryo:2603` | `GenericRegistry::get_template` | `q_sym` | B | a template by the canonical name of a stamped type (`target_key`, `get_qualified_name`, a `Def` stamp, the call's pinned template key) |
| `compiler/sema/call_resolver.cryo:3679` | `GenericRegistry::get_template` | `scope_sym` | B | a template by the canonical name of a stamped type (`target_key`, `get_qualified_name`, a `Def` stamp, the call's pinned template key) |
| `compiler/sema/call_resolver.cryo:5254` | `GenericRegistry::get_template` | `q_sym` | B | a template by the canonical name of a stamped type (`target_key`, `get_qualified_name`, a `Def` stamp, the call's pinned template key) |
| `compiler/sema/method_binding.cryo:910` | `GenericRegistry::get_template` | `recv_qname` | B | a template by the canonical name of a stamped type (`target_key`, `get_qualified_name`, a `Def` stamp, the call's pinned template key) |
| `compiler/sema/sema.cryo:485` | `GenericRegistry::get_template` | `ib.target_key(this.arena)` | B | a template by the canonical name of a stamped type (`target_key`, `get_qualified_name`, a `Def` stamp, the call's pinned template key) |
| `compiler/types/ownership.cryo:796` | `GenericRegistry::get_template` | `qname` | B | a template by the canonical name of a stamped type (`target_key`, `get_qualified_name`, a `Def` stamp, the call's pinned template key) |
| `compiler/types/ownership.cryo:993` | `GenericRegistry::get_template` | `qname` | B | a template by the canonical name of a stamped type (`target_key`, `get_qualified_name`, a `Def` stamp, the call's pinned template key) |
| `compiler/mono/trait_specializer.cryo:113` | `GenericRegistry::get_trait_decl` | ` ...` | B | a trait declaration by its identity string (`tr.identity()`, `trait_identity(ann)`, `qualified_trait_name`) |
| `compiler/passes/type_resolution.cryo:169` | `GenericRegistry::get_trait_decl` | `trait_id` | B | a trait declaration by its identity string (`tr.identity()`, `trait_identity(ann)`, `qualified_trait_name`) |
| `compiler/passes/type_resolution.cryo:235` | `GenericRegistry::get_trait_decl` | `trait_id` | B | a trait declaration by its identity string (`tr.identity()`, `trait_identity(ann)`, `qualified_trait_name`) |
| `compiler/passes/type_resolution.cryo:2044` | `GenericRegistry::get_trait_decl` | `trait_name` | B | a trait declaration by its identity string (`tr.identity()`, `trait_identity(ann)`, `qualified_trait_name`) |
| `compiler/sema/async_lower.cryo:704` | `GenericRegistry::get_trait_decl` | `leaf` | B | a trait declaration by its identity string (`tr.identity()`, `trait_identity(ann)`, `qualified_trait_name`) |
| `compiler/sema/method_binding.cryo:515` | `GenericRegistry::get_trait_decl` | `tr.identity()` | B | a trait declaration by its identity string (`tr.identity()`, `trait_identity(ann)`, `qualified_trait_name`) |
| `compiler/sema/method_binding.cryo:723` | `GenericRegistry::get_trait_decl` | `dimpl.qualified_trait_name` | B | a trait declaration by its identity string (`tr.identity()`, `trait_identity(ann)`, `qualified_trait_name`) |
| `compiler/sema/method_binding.cryo:1057` | `GenericRegistry::get_trait_decl` | `blocks[i].qualified_trait_name` | B | a trait declaration by its identity string (`tr.identity()`, `trait_identity(ann)`, `qualified_trait_name`) |
| `compiler/sema/method_binding.cryo:1162` | `GenericRegistry::get_trait_decl` | `bound_leaves[i]` | B | a trait declaration by its identity string (`tr.identity()`, `trait_identity(ann)`, `qualified_trait_name`) |
| `compiler/sema/sema.cryo:754` | `GenericRegistry::get_trait_decl` | `trait_leaf` | B | a trait declaration by its identity string (`tr.identity()`, `trait_identity(ann)`, `qualified_trait_name`) |
| `compiler/sema/sema.cryo:3828` | `GenericRegistry::get_trait_decl` | `trait_leaf` | B | a trait declaration by its identity string (`tr.identity()`, `trait_identity(ann)`, `qualified_trait_name`) |
| `compiler/types/resolver.cryo:597` | `GenericRegistry::get_trait_decl` | `tr.identity()` | B | a trait declaration by its identity string (`tr.identity()`, `trait_identity(ann)`, `qualified_trait_name`) |
| `compiler/types/resolver.cryo:703` | `GenericRegistry::get_trait_decl` | `tr.identity()` | B | a trait declaration by its identity string (`tr.identity()`, `trait_identity(ann)`, `qualified_trait_name`) |
| `compiler/types/ownership.cryo:762` | `GenericRegistry::heads_for` | `drop_id, ty, intern` | B | the impl heads of a trait identity for a subject `TypeRef` |
| `compiler/types/trait_checker.cryo:183` | `GenericRegistry::heads_for` | `trait_name, subject, this.intern_table` | B | the impl heads of a trait identity for a subject `TypeRef` |
| `compiler/types/trait_checker.cryo:198` | `GenericRegistry::heads_for` | `trait_id, subject, this.intern_table` | B | the impl heads of a trait identity for a subject `TypeRef` |
| `compiler/types/trait_checker.cryo:296` | `GenericRegistry::heads_for` | `trait_name, ty, this.intern_table` | B | the impl heads of a trait identity for a subject `TypeRef` |
| `compiler/sema/call_resolver.cryo:2921` | `GenericRegistry::inherent_impl_blocks` | `owner_sym` | B | a primitive's `implement` blocks by the spelling the stamp carries (`scope_owner_key`) |
| `compiler/types/ownership.cryo:818` | `GenericRegistry::inherent_impl_has_method` | `qname, drop_sym` | B | `(owner, method)`: the owner a canonical name off a `TypeRef`, the method a leaf inside it |
| `compiler/mono/call_specializer.cryo:1816` | `GenericRegistry::lookup_inherent_owner` | `qname` | B | the declaring node of a type by the canonical name off its `TypeRef` |
| `compiler/sema/method_binding.cryo:908` | `GenericRegistry::lookup_inherent_owner` | `recv_qname` | B | the declaring node of a type by the canonical name off its `TypeRef` |
| `compiler/sema/method_binding.cryo:1364` | `GenericRegistry::lookup_inherent_owner` | `recv_qname` | B | the declaring node of a type by the canonical name off its `TypeRef` |
| `compiler/types/ownership.cryo:808` | `GenericRegistry::lookup_inherent_owner` | `qname` | B | the declaring node of a type by the canonical name off its `TypeRef` |
| `compiler/types/ownership.cryo:1004` | `GenericRegistry::lookup_inherent_owner` | `qname` | B | the declaring node of a type by the canonical name off its `TypeRef` |
| `compiler/passes/type_resolution.cryo:1180` | `GenericRegistry::overlapping_head` | ` ...` | B | a head that overlaps `(trait identity, target key)` at registration |
| `compiler/passes/type_resolution.cryo:1820` | `GenericRegistry::select_trait_impl` | `tr.identity(), item_ty, ctx.intern_table` | B | the selected head of a trait identity for a subject `TypeRef` |
| `compiler/sema/method_binding.cryo:787` | `GenericRegistry::select_trait_impl` | `tr.identity(), item_ty, this.intern` | B | the selected head of a trait identity for a subject `TypeRef` |
| `compiler/mono/trait_specializer.cryo:137` | `GenericRegistry::template_head_for` | `trait_id, subject, this.intern_table` | B | the written head of a trait identity for a subject `TypeRef` |
| `compiler/sema/method_binding.cryo:1691` | `GenericRegistry::template_head_for` | `trait_id, subject, this.intern` | B | the written head of a trait identity for a subject `TypeRef` |
| `compiler/types/resolver.cryo:518` | `GenericRegistry::template_head_for` | ` ...` | B | the written head of a trait identity for a subject `TypeRef` |
| `compiler/codegen/visit/call_emitter.cryo:1233` | `DeclarationIndex::family_is_intrinsic` | `f` | C | a family by its key string, asked whether every entry is an intrinsic (the `CalleePin::Family` arm; the `Decl` arm reads the mark off the entry) |
| `compiler/codegen/ops/symbol_resolver.cryo:227` | `DeclarationIndex::lookup_family_entries` | `name` | C | a FAMILY by its key string - the overload set under one qualified name; pin-as-declaration retires the family door |
| `compiler/sema/call_resolver.cryo:2513` | `DeclarationIndex::lookup_family_entries` | `family` | C | a FAMILY by its key string - the overload set under one qualified name; pin-as-declaration retires the family door |
| `compiler/sema/call_resolver.cryo:3750` | `DeclarationIndex::lookup_family_entries` | `sym` | C | a FAMILY by its key string - the overload set under one qualified name; pin-as-declaration retires the family door |
| `compiler/sema/call_resolver.cryo:3835` | `DeclarationIndex::lookup_family_entries` | `family` | C | a FAMILY by its key string - the overload set under one qualified name; pin-as-declaration retires the family door |
| `compiler/sema/call_resolver.cryo:4010` | `DeclarationIndex::lookup_family_entries` | `family` | C | a FAMILY by its key string - the overload set under one qualified name; pin-as-declaration retires the family door |
| `compiler/sema/call_resolver.cryo:4128` | `DeclarationIndex::lookup_family_entries` | `combined_sym` | C | a FAMILY by its key string - the overload set under one qualified name; pin-as-declaration retires the family door |
| `compiler/sema/call_resolver.cryo:956` | `DeclarationIndex::lookup_func_type` | `fam` | C | the `CalleePin::Family` key string |
| `compiler/sema/call_resolver.cryo:1877` | `DeclarationIndex::lookup_func_type_overloads` | `family` | C | a family by its key string (see `lookup_family_entries`) |
| `compiler/sema/call_resolver.cryo:5624` | `DeclarationIndex::lookup_func_type_overloads` | `combined` | C | a family by its key string (see `lookup_family_entries`) |
| `compiler/sema/call_resolver.cryo:3392` | `DeclarationIndex::signature_refused` | `this.intern.intern( ...` | C | `Owner::member` composed from the owner key and the written member |
| `compiler/sema/call_resolver.cryo:3666` | `DeclarationIndex::signature_refused` | `combined` | C | `Owner::member` composed from the owner key and the written member |
| `compiler/mono/call_specializer.cryo:2370` | `GenericRegistry::get_template` | `this.intern_table.intern(q_str)` | C | `ns::name` composed from a namespace and a leaf |
| `compiler/sema/call_resolver.cryo:3685` | `GenericRegistry::get_template` | `csym` | C | `scope::member` composed from the owner key and the written member |
| `compiler/sema/call_resolver.cryo:4307` | `GenericRegistry::get_template` | `this.intern.intern(qstr)` | C | `owner::member` composed from `scope_owner_key` and the written member |
| `compiler/sema/call_resolver.cryo:311` | `TypeUtils::lookup_func_type_exact` | `q_sym` | C | a family or a `module::member` composed by `resolve_module_qualified_symbol` |
| `compiler/sema/call_resolver.cryo:2593` | `TypeUtils::lookup_func_type_exact` | `q_sym` | C | a family or a `module::member` composed by `resolve_module_qualified_symbol` |
| `compiler/sema/call_resolver.cryo:2650` | `TypeUtils::lookup_func_type_exact` | `family` | C | a family or a `module::member` composed by `resolve_module_qualified_symbol` |
| `compiler/sema/call_resolver.cryo:5221` | `TypeUtils::lookup_func_type_exact` | `q_sym` | C | a family or a `module::member` composed by `resolve_module_qualified_symbol` |
| `compiler/sema/call_resolver.cryo:2559` | `DeclarationIndex::is_candidate_public` | `callee` | F | `enforce_callee_visibility` asks the callee's visibility by the callee's name |
| `compiler/sema/call_resolver.cryo:2556` | `DeclarationIndex::namespace_of` | `callee, this.intern` | F | `enforce_callee_visibility` asks the callee's module by the callee's name |
| `compiler/codegen/type_map.cryo:316` | `ClassType::field_index` | `member` | J | member: a class's field position by its leaf off the `ClassType` in hand |
| `compiler/mono/call_specializer.cryo:749` | `ClassType::get_field` | `ma.member` | J | member: a class's field by its leaf off the `ClassType` in hand |
| `compiler/sema/member_resolver.cryo:711` | `ClassType::get_field` | `field_name` | J | member: a class's field by its leaf off the `ClassType` in hand |
| `compiler/sema/sema.cryo:2971` | `ClassType::get_field` | `field_init.field_name` | J | member: a class's field by its leaf off the `ClassType` in hand |
| `compiler/types/checker.cryo:1045` | `ClassType::get_field` | `field_name` | J | member: a class's field by its leaf off the `ClassType` in hand |
| `compiler/sema/diagnostics.cryo:314` | `ClassType::get_method` | `member_name` | J | member: a class's method by its leaf off the `ClassType` in hand |
| `compiler/sema/member_resolver.cryo:777` | `ClassType::get_method` | `method_name` | J | member: a class's method by its leaf off the `ClassType` in hand |
| `compiler/sema/sema.cryo:3150` | `ClassType::get_method` | `scope.member_name` | J | member: a class's method by its leaf off the `ClassType` in hand |
| `compiler/passes/type_resolution.cryo:2474` | `DeclarationIndex::extern_symbol_conflict` | `ext_link_sym, func_type_ref` | J | extern: a C symbol by its link name, the only identity a C symbol has |
| `compiler/codegen/ops/symbol_resolver.cryo:299` | `DeclarationIndex::global_entry_in_module` | `name, module` | J | member: a global's leaf inside the module the qualifier's stamp names (a `DefId`) |
| `compiler/sema/type_utils.cryo:149` | `DeclarationIndex::global_entry_in_module` | `scope.member_name, seen.def_id()` | J | member: a global's leaf inside the module the qualifier's stamp names (a `DefId`) |
| `compiler/compilation_context.cryo:522` | `DeclarationIndex::is_prelude_ns` | `cand_ns` | J | module: a namespace asked whether it is the prelude's; a module's identity is its path |
| `compiler/resolver/name_resolution.cryo:1466` | `DeclarationIndex::is_prelude_ns` | `owner_ns` | J | module: a namespace asked whether it is the prelude's; a module's identity is its path |
| `compiler/codegen/test_main_codegen.cryo:347` | `DeclarationIndex::lookup_sole_entry` | `set_args_qualified` | J | lang: the test runner's entry points `std::env::set_args` / `set_env` / `std::test::runner::run_all`, named by the language's own paths |
| `compiler/codegen/test_main_codegen.cryo:348` | `DeclarationIndex::lookup_sole_entry` | `set_env_qualified` | J | lang: the test runner's entry points `std::env::set_args` / `set_env` / `std::test::runner::run_all`, named by the language's own paths |
| `compiler/codegen/test_main_codegen.cryo:349` | `DeclarationIndex::lookup_sole_entry` | `run_all_qualified` | J | lang: the test runner's entry points `std::env::set_args` / `set_env` / `std::test::runner::run_all`, named by the language's own paths |
| `compiler/passes/type_resolution.cryo:1862` | `DeclarationIndex::lookup_type` | `n` | J | prim: `Res::PrimTy(n)` - the primitive's identity is the spelling the stamp carries |
| `compiler/sema/async_lower.cryo:584` | `DeclarationIndex::lookup_type` | `this.intern.intern(qualified)` | J | lang: `lookup_future_type` - the `std::future` declarations the lowering names by their own paths |
| `compiler/compilation_context.cryo:523` | `DeclarationIndex::ns_imports` | `use_ns, cand_ns` | J | module: two namespaces asked whether one imports the other; module identities |
| `compiler/resolver/name_resolution.cryo:1467` | `DeclarationIndex::ns_imports` | `use_ns, owner_ns` | J | module: two namespaces asked whether one imports the other; module identities |
| `compiler/sema/call_resolver.cryo:3659` | `DeclarationIndex::signature_refused_in_module` | `scope.member_name, ...` | J | member: a function's leaf inside the module the qualifier's stamp names (a `DefId`) |
| `compiler/sema/diagnostics.cryo:319` | `EnumType::get_method` | `member_name` | J | member: an enum's method by its leaf off the `EnumType` in hand |
| `compiler/codegen/ops/declaration_emitter.cryo:2371` | `EnumType::get_variant` | `sr.member_name` | J | member: an enum's variant by its leaf off the `EnumType` in hand |
| `compiler/codegen/visit/call_emitter.cryo:508` | `EnumType::get_variant` | `sr.member_name` | J | member: an enum's variant by its leaf off the `EnumType` in hand |
| `compiler/codegen/visit/ir_generator.cryo:1644` | `EnumType::get_variant` | `node.member_name` | J | member: an enum's variant by its leaf off the `EnumType` in hand |
| `compiler/codegen/visit/new_delete_emitter.cryo:160` | `EnumType::get_variant` | `node.variant_name` | J | member: an enum's variant by its leaf off the `EnumType` in hand |
| `compiler/codegen/visit/pattern_emitter.cryo:284` | `EnumType::get_variant` | `epat.variant_name` | J | member: an enum's variant by its leaf off the `EnumType` in hand |
| `compiler/codegen/visit/pattern_emitter.cryo:477` | `EnumType::get_variant` | `epat.variant_name` | J | member: an enum's variant by its leaf off the `EnumType` in hand |
| `compiler/codegen/visit/pattern_emitter.cryo:547` | `EnumType::get_variant` | `epat.variant_name` | J | member: an enum's variant by its leaf off the `EnumType` in hand |
| `compiler/sema/call_resolver.cryo:550` | `EnumType::get_variant` | `scope.member_name` | J | member: an enum's variant by its leaf off the `EnumType` in hand |
| `compiler/sema/call_resolver.cryo:5098` | `EnumType::get_variant` | `member` | J | member: an enum's variant by its leaf off the `EnumType` in hand |
| `compiler/sema/diagnostics.cryo:319` | `EnumType::get_variant` | `member_name` | J | member: an enum's variant by its leaf off the `EnumType` in hand |
| `compiler/sema/pattern_resolver.cryo:168` | `EnumType::get_variant` | `enum_pat.variant_name` | J | member: an enum's variant by its leaf off the `EnumType` in hand |
| `compiler/sema/sema.cryo:2693` | `EnumType::get_variant` | `this.intern.intern("Ready")` | J | lang: `Poll::Ready`, the variant the async protocol names, inside the enum a future's `poll` returns |
| `compiler/sema/sema.cryo:3223` | `EnumType::get_variant` | `scope.member_name` | J | member: an enum's variant by its leaf off the `EnumType` in hand |
| `compiler/sema/method_binding.cryo:816` | `GenericRegistry::find_trait_defining_method` | `method_name` | J | hint: the did-you-mean asks which trait declares a method of this leaf; the spelling is the question |
| `compiler/mono/monomorphizer.cryo:302` | `GenericRegistry::get_template` | `array_sym` | J | lang: `std::collections::array::Array`, the type the language lowers `T[]` to, by its own path |
| `compiler/mono/state.cryo:404` | `GenericRegistry::get_template` | `array_sym` | J | lang: `std::collections::array::Array`, the type the language lowers `T[]` to, by its own path |
| `compiler/passes/move_check.cryo:433` | `GenericRegistry::wellknown` | `this.intern.intern("Future")` | J | lang: a declaration the language names by its own leaf (`Drop`, `Copy`, `Send`, `Sync`, `Future`, `Deref`, `Index`, the operator traits, `Result`, `Option`), claimed at its declaration |
| `compiler/sema/async_lower.cryo:6753` | `GenericRegistry::wellknown` | `future_leaf` | J | lang: a declaration the language names by its own leaf (`Drop`, `Copy`, `Send`, `Sync`, `Future`, `Deref`, `Index`, the operator traits, `Result`, `Option`), claimed at its declaration |
| `compiler/sema/member_resolver.cryo:265` | `GenericRegistry::wellknown` | `deref_leaf` | J | lang: a declaration the language names by its own leaf (`Drop`, `Copy`, `Send`, `Sync`, `Future`, `Deref`, `Index`, the operator traits, `Result`, `Option`), claimed at its declaration |
| `compiler/sema/member_resolver.cryo:557` | `GenericRegistry::wellknown` | `this.intern.intern("Index")` | J | lang: a declaration the language names by its own leaf (`Drop`, `Copy`, `Send`, `Sync`, `Future`, `Deref`, `Index`, the operator traits, `Result`, `Option`), claimed at its declaration |
| `compiler/sema/sema.cryo:1813` | `GenericRegistry::wellknown` | `this.intern.intern(map.trait_leaf)` | J | lang: a declaration the language names by its own leaf (`Drop`, `Copy`, `Send`, `Sync`, `Future`, `Deref`, `Index`, the operator traits, `Result`, `Option`), claimed at its declaration |
| `compiler/sema/sema.cryo:1901` | `GenericRegistry::wellknown` | `trait_leaf` | J | lang: a declaration the language names by its own leaf (`Drop`, `Copy`, `Send`, `Sync`, `Future`, `Deref`, `Index`, the operator traits, `Result`, `Option`), claimed at its declaration |
| `compiler/sema/sema.cryo:2684` | `GenericRegistry::wellknown` | `this.intern.intern("Future")` | J | lang: a declaration the language names by its own leaf (`Drop`, `Copy`, `Send`, `Sync`, `Future`, `Deref`, `Index`, the operator traits, `Result`, `Option`), claimed at its declaration |
| `compiler/sema/sema.cryo:2717` | `GenericRegistry::wellknown` | `this.intern.intern("Result")` | J | lang: a declaration the language names by its own leaf (`Drop`, `Copy`, `Send`, `Sync`, `Future`, `Deref`, `Index`, the operator traits, `Result`, `Option`), claimed at its declaration |
| `compiler/sema/sema.cryo:2719` | `GenericRegistry::wellknown` | `this.intern.intern("Option")` | J | lang: a declaration the language names by its own leaf (`Drop`, `Copy`, `Send`, `Sync`, `Future`, `Deref`, `Index`, the operator traits, `Result`, `Option`), claimed at its declaration |
| `compiler/types/ownership.cryo:761` | `GenericRegistry::wellknown` | `intern.intern("Drop")` | J | lang: a declaration the language names by its own leaf (`Drop`, `Copy`, `Send`, `Sync`, `Future`, `Deref`, `Index`, the operator traits, `Result`, `Option`), claimed at its declaration |
| `compiler/types/trait_checker.cryo:106` | `GenericRegistry::wellknown` | `this.intern_table.intern("Copy")` | J | lang: a declaration the language names by its own leaf (`Drop`, `Copy`, `Send`, `Sync`, `Future`, `Deref`, `Index`, the operator traits, `Result`, `Option`), claimed at its declaration |
| `compiler/types/trait_checker.cryo:116` | `GenericRegistry::wellknown` | `this.intern_table.intern("Send")` | J | lang: a declaration the language names by its own leaf (`Drop`, `Copy`, `Send`, `Sync`, `Future`, `Deref`, `Index`, the operator traits, `Result`, `Option`), claimed at its declaration |
| `compiler/types/trait_checker.cryo:121` | `GenericRegistry::wellknown` | `this.intern_table.intern("Sync")` | J | lang: a declaration the language names by its own leaf (`Drop`, `Copy`, `Send`, `Sync`, `Future`, `Deref`, `Index`, the operator traits, `Result`, `Option`), claimed at its declaration |
| `compiler/types/trait_checker.cryo:133` | `GenericRegistry::wellknown` | `this.intern_table.intern("Drop")` | J | lang: a declaration the language names by its own leaf (`Drop`, `Copy`, `Send`, `Sync`, `Future`, `Deref`, `Index`, the operator traits, `Result`, `Option`), claimed at its declaration |
| `compiler/passes/type_resolution.cryo:204` | `ImplBlockNode::lookup_assoc_binding` | `adecl.name` | J | member: the impl's own `This::Member` binding by the member's leaf, asked from inside the block |
| `compiler/passes/type_resolution.cryo:281` | `ImplBlockNode::lookup_assoc_binding` | `adecl.name` | J | member: the impl's own `This::Member` binding by the member's leaf, asked from inside the block |
| `compiler/sema/async_lower.cryo:710` | `ImplBlockNode::lookup_assoc_binding` | `assoc` | J | member: the impl's own `This::Member` binding by the member's leaf, asked from inside the block |
| `compiler/types/resolver.cryo:534` | `ImplBlockNode::lookup_assoc_binding` | `member` | J | member: the impl's own `This::Member` binding by the member's leaf, asked from inside the block |
| `compiler/instance.cryo:890` | `ModuleGraph::find_module_index` | `ctx.intern_table.intern(ns)` | J | module: a module by its namespace path |
| `compiler/instance.cryo:3018` | `ModuleGraph::find_module_index` | `qual` | J | module: a module by its namespace path |
| `compiler/module_loader.cryo:644` | `ModuleGraph::find_module_index` | `already_name` | J | module: a module by its namespace path |
| `compiler/module_loader.cryo:775` | `ModuleGraph::find_module_index` | `item_sym` | J | module: a module by its namespace path |
| `compiler/module_loader.cryo:834` | `ModuleGraph::find_module_index` | `exp_sym` | J | module: a module by its namespace path |
| `compiler/module_loader.cryo:891` | `ModuleGraph::find_module_index` | `eitem_sym` | J | module: a module by its namespace path |
| `compiler/passes/pass_registry.cryo:1216` | `ModuleGraph::find_module_index` | `mod_sym` | J | module: a module by its namespace path |
| `compiler/resolver/name_resolution.cryo:536` | `ModuleGraph::find_module_index` | `canonical` | J | module: a module by its namespace path |
| `compiler/resolver/name_resolution.cryo:3106` | `ModuleGraph::find_module_index` | `module_sym` | J | module: a module by its namespace path |
| `compiler/resolver/name_resolution.cryo:3253` | `ModuleGraph::find_module_index` | `sub_sym` | J | module: a module by its namespace path |
| `compiler/resolver/name_resolution.cryo:1362` | `ModuleGraph::reexport_closure` | `cand.name` | J | module: a module's re-export closure by its namespace path |
| `compiler/resolver/name_resolution.cryo:2897` | `ModuleGraph::reexport_closure` | `m` | J | module: a module's re-export closure by its namespace path |
| `compiler/resolver/name_resolution.cryo:2945` | `ModuleGraph::reexport_closure` | `m` | J | module: a module's re-export closure by its namespace path |
| `compiler/resolver/name_resolution.cryo:2993` | `ModuleGraph::reexport_closure` | `module_sym` | J | module: a module's re-export closure by its namespace path |
| `compiler/resolver/name_resolution.cryo:3152` | `ModuleGraph::reexport_closure` | `module_sym` | J | module: a module's re-export closure by its namespace path |
| `compiler/instance.cryo:1839` | `ModuleGraph::source_file_for_owner_key` | ` ...` | J | module: a module's source file by its namespace path |
| `compiler/passes/specialization.cryo:522` | `ModuleGraph::source_file_for_owner_key` | ` ...` | J | module: a module's source file by its namespace path |
| `compiler/passes/specialization.cryo:598` | `ModuleGraph::source_file_for_owner_key` | ` ...` | J | module: a module's source file by its namespace path |
| `compiler/mono/monomorphizer.cryo:602` | `Resolver::find_module_scope` | `entry.module_name` | J | module: a module's scope by its namespace path, to stand in it while a template is instantiated |
| `compiler/sema/sema.cryo:519` | `Resolver::find_module_scope` | `template_mod` | J | module: a module's scope by its namespace path, to stand in it while a template is instantiated |
| `compiler/codegen/type_map.cryo:299` | `StructType::field_index` | `member` | J | member: a struct's field position by its leaf off the `StructType` in hand |
| `compiler/codegen/visit/ir_generator.cryo:1132` | `StructType::field_index` | `field_names[0]` | J | member: a struct's field position by its leaf off the `StructType` in hand |
| `compiler/codegen/visit/new_delete_emitter.cryo:268` | `StructType::field_index` | `finit.field_name` | J | member: a struct's field position by its leaf off the `StructType` in hand |
| `compiler/codegen/visit/place_emitter.cryo:163` | `StructType::field_index` | `member` | J | member: a struct's field position by its leaf off the `StructType` in hand |
| `compiler/mono/call_specializer.cryo:746` | `StructType::get_field` | `ma.member` | J | member: a struct's field by its leaf off the `StructType` in hand; Rust's field lookup is name-keyed off the owner too |
| `compiler/sema/async_lower.cryo:2443` | `StructType::get_field` | `this.recv_sym` | J | member: the receiver field the lowering minted on the frame struct, asked back by the spelling it minted |
| `compiler/sema/member_resolver.cryo:686` | `StructType::get_field` | `this.intern.intern("this$recv")` | J | member: the receiver field the async lowering minted on the frame struct, asked back by the spelling it minted |
| `compiler/sema/member_resolver.cryo:706` | `StructType::get_field` | `field_name` | J | member: a struct's field by its leaf off the `StructType` in hand; Rust's field lookup is name-keyed off the owner too |
| `compiler/sema/sema.cryo:2970` | `StructType::get_field` | `field_init.field_name` | J | member: a struct's field by its leaf off the `StructType` in hand; Rust's field lookup is name-keyed off the owner too |
| `compiler/types/checker.cryo:1032` | `StructType::get_field` | `field_name` | J | member: a struct's field by its leaf off the `StructType` in hand; Rust's field lookup is name-keyed off the owner too |
| `compiler/sema/diagnostics.cryo:310` | `StructType::get_method` | `member_name` | J | member: a struct's method by its leaf off the `StructType` in hand; Rust's method lookup is name-keyed off the owner too |
| `compiler/sema/lambda_synth.cryo:475` | `StructType::get_method` | `this.intern.intern("__call__")` | J | lang: the call protocol's `__call__`, the method leaf the language fixes for a callable struct |
| `compiler/sema/member_resolver.cryo:772` | `StructType::get_method` | `method_name` | J | member: a struct's method by its leaf off the `StructType` in hand; Rust's method lookup is name-keyed off the owner too |
| `compiler/sema/sema.cryo:3149` | `StructType::get_method` | `scope.member_name` | J | member: a struct's method by its leaf off the `StructType` in hand; Rust's method lookup is name-keyed off the owner too |
| `compiler/types/checker.cryo:331` | `StructType::get_method` | ` ...` | J | lang: the call protocol's `__call__` (the argument continues on the next line), asked when a struct converts to a function type |
| `compiler/passes/type_resolution.cryo:1718` | `TraitDeclNode::lookup_assoc_type` | `assoc` | J | member: the trait's own associated type by its leaf, asked from the trait node in hand |
| `compiler/sema/async_lower.cryo:709` | `TraitDeclNode::lookup_assoc_type` | `assoc` | J | member: the trait's own associated type by its leaf, asked from the trait node in hand |
| `compiler/resolver/name_resolution.cryo:2851` | `ConstantTable::intern_qualified` | `intern, ns, node.name` | N | a key MINTED at the constant's declaration (`ns::name`) for its registration; the write side |
| `compiler/resolver/name_resolution.cryo:2881` | `ConstantTable::intern_qualified` | `intern, ns, node.name` | N | a key MINTED at the constant's declaration (`ns::name`) for its registration; the write side |
| `compiler/passes/drop_insertion.cryo:3319` | `ConstantTable::parse_int_literal` | `text, &v` | N | a literal's text parsed to a value |
| `compiler/codegen/ops/declaration_emitter.cryo:641` | `DeclarationIndex::impl_owner` | `node, "codegen/declare impl"` | N | keyed by the impl node; the string is the diagnostic label of the asking site |
| `compiler/codegen/visit/decl_visit_emitter.cryo:217` | `DeclarationIndex::impl_owner` | `node, "codegen/impl bodies"` | N | keyed by the impl node; the string is the diagnostic label of the asking site |
| `compiler/passes/type_resolution.cryo:1208` | `DeclarationIndex::impl_owner` | `node, "type_resolution/register impl methods"` | N | keyed by the impl node; the string is the diagnostic label of the asking site |
| `compiler/passes/type_resolution.cryo:2408` | `DeclarationIndex::impl_owner` | `node, "type_resolution/impl methods"` | N | keyed by the impl node; the string is the diagnostic label of the asking site |
| `compiler/passes/type_resolution.cryo:2747` | `DeclarationIndex::impl_owner` | `node, "type_resolution/impl methods"` | N | keyed by the impl node; the string is the diagnostic label of the asking site |
| `compiler/passes/type_resolution.cryo:3466` | `DeclarationIndex::impl_owner` | `node, "type_resolution/struct field sync"` | N | keyed by the impl node; the string is the diagnostic label of the asking site |
| `compiler/sema/sema.cryo:380` | `DeclarationIndex::impl_owner` | `ib, "sema/async declare"` | N | keyed by the impl node; the string is the diagnostic label of the asking site |
| `compiler/sema/sema.cryo:630` | `DeclarationIndex::impl_owner` | `node, "sema/visit:ImplBlockNode"` | N | keyed by the impl node; the string is the diagnostic label of the asking site |
| `compiler/sema/sema.cryo:662` | `DeclarationIndex::impl_owner` | `node, "sema/visit:ImplBlockNode"` | N | keyed by the impl node; the string is the diagnostic label of the asking site |
| `compiler/sema/sema.cryo:683` | `DeclarationIndex::impl_owner` | `node, "sema/visit:ImplBlockNode"` | N | keyed by the impl node; the string is the diagnostic label of the asking site |
| `compiler/sema/type_utils.cryo:212` | `DeclarationIndex::lookup_func_type` | `name` | N | the funnel's own forwarding body (`lookup_func_type_exact`); its callers are the sites |
| `compiler/sema/type_utils.cryo:189` | `DeclarationIndex::lookup_method_return` | `type_sym, method_sym` | N | the funnel's own forwarding body (`resolve_method_owner`); its callers are the sites |
| `compiler/sema/type_utils.cryo:205` | `DeclarationIndex::lookup_type` | `name` | N | the funnel's own forwarding body (`lookup_type_exact`); its callers are the sites |
| `compiler/codegen/ops/declaration_emitter.cryo:233` | `DeclarationIndex::type_of_decl` | `node.def, "codegen/declare struct type"` | N | keyed by the `DefId`; the string is the diagnostic label of the asking site |
| `compiler/codegen/ops/declaration_emitter.cryo:249` | `DeclarationIndex::type_of_decl` | `node.def, "codegen/declare union type"` | N | keyed by the `DefId`; the string is the diagnostic label of the asking site |
| `compiler/codegen/ops/declaration_emitter.cryo:262` | `DeclarationIndex::type_of_decl` | `node.def, "codegen/declare class type"` | N | keyed by the `DefId`; the string is the diagnostic label of the asking site |
| `compiler/codegen/ops/declaration_emitter.cryo:276` | `DeclarationIndex::type_of_decl` | `node.def, "codegen/declare enum type"` | N | keyed by the `DefId`; the string is the diagnostic label of the asking site |
| `compiler/codegen/ops/declaration_emitter.cryo:655` | `DeclarationIndex::type_of_decl` | `node.def, "codegen/declare struct methods"` | N | keyed by the `DefId`; the string is the diagnostic label of the asking site |
| `compiler/codegen/ops/declaration_emitter.cryo:670` | `DeclarationIndex::type_of_decl` | `node.def, "codegen/declare union methods"` | N | keyed by the `DefId`; the string is the diagnostic label of the asking site |
| `compiler/codegen/ops/declaration_emitter.cryo:681` | `DeclarationIndex::type_of_decl` | `node.def, "codegen/declare class methods"` | N | keyed by the `DefId`; the string is the diagnostic label of the asking site |
| `compiler/codegen/visit/decl_visit_emitter.cryo:113` | `DeclarationIndex::type_of_decl` | `node.def, "codegen/struct method bodies"` | N | keyed by the `DefId`; the string is the diagnostic label of the asking site |
| `compiler/codegen/visit/decl_visit_emitter.cryo:127` | `DeclarationIndex::type_of_decl` | `node.def, "codegen/union method bodies"` | N | keyed by the `DefId`; the string is the diagnostic label of the asking site |
| `compiler/codegen/visit/decl_visit_emitter.cryo:141` | `DeclarationIndex::type_of_decl` | `node.def, "codegen/class method bodies"` | N | keyed by the `DefId`; the string is the diagnostic label of the asking site |
| `compiler/passes/directive_processing.cryo:1703` | `DeclarationIndex::type_of_decl` | `node.def, "directive/struct layout"` | N | keyed by the `DefId`; the string is the diagnostic label of the asking site |
| `compiler/passes/directive_processing.cryo:1720` | `DeclarationIndex::type_of_decl` | `node.def, "directive/union layout"` | N | keyed by the `DefId`; the string is the diagnostic label of the asking site |
| `compiler/passes/directive_processing.cryo:1735` | `DeclarationIndex::type_of_decl` | `node.def, "directive/class layout"` | N | keyed by the `DefId`; the string is the diagnostic label of the asking site |
| `compiler/passes/directive_processing.cryo:1747` | `DeclarationIndex::type_of_decl` | `node.def, "directive/enum layout"` | N | keyed by the `DefId`; the string is the diagnostic label of the asking site |
| `compiler/passes/type_resolution.cryo:974` | `DeclarationIndex::type_of_decl` | `node.def, "type_resolution/register struct methods"` | N | keyed by the `DefId`; the string is the diagnostic label of the asking site |
| `compiler/passes/type_resolution.cryo:978` | `DeclarationIndex::type_of_decl` | `node.def, "type_resolution/register union methods"` | N | keyed by the `DefId`; the string is the diagnostic label of the asking site |
| `compiler/passes/type_resolution.cryo:982` | `DeclarationIndex::type_of_decl` | `node.def, "type_resolution/register class methods"` | N | keyed by the `DefId`; the string is the diagnostic label of the asking site |
| `compiler/passes/type_resolution.cryo:2259` | `DeclarationIndex::type_of_decl` | `node.def, "type_resolution/struct methods"` | N | keyed by the `DefId`; the string is the diagnostic label of the asking site |
| `compiler/passes/type_resolution.cryo:2268` | `DeclarationIndex::type_of_decl` | `node.def, "type_resolution/union methods"` | N | keyed by the `DefId`; the string is the diagnostic label of the asking site |
| `compiler/passes/type_resolution.cryo:2277` | `DeclarationIndex::type_of_decl` | `node.def, "type_resolution/class methods"` | N | keyed by the `DefId`; the string is the diagnostic label of the asking site |
| `compiler/passes/type_resolution.cryo:2310` | `DeclarationIndex::type_of_decl` | `node.def, "type_resolution/trait populate"` | N | keyed by the `DefId`; the string is the diagnostic label of the asking site |
| `compiler/passes/type_resolution.cryo:2642` | `DeclarationIndex::type_of_decl` | `node.def, "type_resolution/alias target"` | N | keyed by the `DefId`; the string is the diagnostic label of the asking site |
| `compiler/passes/type_resolution.cryo:3091` | `DeclarationIndex::type_of_decl` | `node.def, "type_resolution/struct field sync"` | N | keyed by the `DefId`; the string is the diagnostic label of the asking site |
| `compiler/passes/type_resolution.cryo:3169` | `DeclarationIndex::type_of_decl` | `node.def, "type_resolution/union field sync"` | N | keyed by the `DefId`; the string is the diagnostic label of the asking site |
| `compiler/passes/type_resolution.cryo:3232` | `DeclarationIndex::type_of_decl` | `node.def, "type_resolution/class field sync"` | N | keyed by the `DefId`; the string is the diagnostic label of the asking site |
| `compiler/passes/type_resolution.cryo:3350` | `DeclarationIndex::type_of_decl` | `node.def, "type_resolution/enum field sync"` | N | keyed by the `DefId`; the string is the diagnostic label of the asking site |
| `compiler/sema/sema.cryo:362` | `DeclarationIndex::type_of_decl` | `s.def, "sema/async declare struct"` | N | keyed by the `DefId`; the string is the diagnostic label of the asking site |
| `compiler/sema/sema.cryo:367` | `DeclarationIndex::type_of_decl` | `u.def, "sema/async declare union"` | N | keyed by the `DefId`; the string is the diagnostic label of the asking site |
| `compiler/sema/sema.cryo:372` | `DeclarationIndex::type_of_decl` | `c.def, "sema/async declare class"` | N | keyed by the `DefId`; the string is the diagnostic label of the asking site |
| `compiler/sema/sema.cryo:579` | `DeclarationIndex::type_of_decl` | `node.def, "sema/visit:StructDeclNode"` | N | keyed by the `DefId`; the string is the diagnostic label of the asking site |
| `compiler/sema/sema.cryo:588` | `DeclarationIndex::type_of_decl` | `node.def, "sema/visit:UnionDeclNode"` | N | keyed by the `DefId`; the string is the diagnostic label of the asking site |
| `compiler/sema/sema.cryo:597` | `DeclarationIndex::type_of_decl` | `node.def, "sema/visit:ClassDeclNode"` | N | keyed by the `DefId`; the string is the diagnostic label of the asking site |
| `compiler/mono/specializer.cryo:234` | `ImplBlockNode::set_target_type` | `spec_sym` | N | a write: the specialized impl's target name stored on the node |
| `compiler/passes/specialization.cryo:683` | `ImplBlockNode::set_target_type` | `impl_qname` | N | a write: the specialized impl's target name stored on the node |
| `compiler/AST/node_locator.cryo:326` | `ModuleGraph::find_module_by_path` | `file_path` | N | a module by its FILE PATH |
| `compiler/AST/node_locator.cryo:467` | `ModuleGraph::find_module_by_path` | `file_path` | N | a module by its FILE PATH |
| `compiler/AST/node_locator.cryo:499` | `ModuleGraph::find_module_by_path` | `target.span.file` | N | a module by its FILE PATH |
| `compiler/AST/node_locator.cryo:1433` | `ModuleGraph::find_module_by_path` | `file` | N | a module by its FILE PATH |
| `compiler/codegen/passes.cryo:1371` | `ModuleGraph::find_module_by_path` | `ctx.source_file` | N | a module by its FILE PATH |
| `compiler/instance.cryo:1821` | `ModuleGraph::find_module_by_path` | ` ...` | N | a module by its FILE PATH |
| `compiler/instance.cryo:1842` | `ModuleGraph::find_module_by_path` | ` ...` | N | a module by its FILE PATH |
| `compiler/module_loader.cryo:700` | `ModuleGraph::find_module_by_path` | `resolved` | N | a module by its FILE PATH |
| `compiler/module_loader.cryo:755` | `ModuleGraph::find_module_by_path` | `item_resolved` | N | a module by its FILE PATH |
| `compiler/module_loader.cryo:813` | `ModuleGraph::find_module_by_path` | `exp_resolved` | N | a module by its FILE PATH |
| `compiler/module_loader.cryo:874` | `ModuleGraph::find_module_by_path` | `eitem_resolved` | N | a module by its FILE PATH |
| `compiler/module_loader.cryo:922` | `ModuleGraph::find_module_by_path` | `sub_resolved` | N | a module by its FILE PATH |
| `compiler/compilation_context.cryo:471` | `ModuleGraph::ns_sym_of_file` | `file` | N | a namespace by its FILE PATH |
| `compiler/codegen/visit/lambda_emitter.cryo:127` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/codegen/visit/lambda_emitter.cryo:142` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/mono/ast_resolver.cryo:107` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/mono/ast_resolver.cryo:181` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/mono/call_specializer.cryo:162` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/mono/call_specializer.cryo:238` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/mono/call_specializer.cryo:1157` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/mono/call_specializer.cryo:1171` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/mono/call_specializer.cryo:1247` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/mono/call_specializer.cryo:1400` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/mono/call_specializer.cryo:1508` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/mono/call_specializer.cryo:1512` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/mono/call_specializer.cryo:1548` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/mono/call_specializer.cryo:1721` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/mono/call_specializer.cryo:2045` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/mono/monomorphizer.cryo:317` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/mono/state.cryo:345` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/mono/state.cryo:408` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/mono/trait_specializer.cryo:147` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/passes/type_resolution.cryo:111` | `ResolutionContext::new` | `ctx.source_file` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/passes/type_resolution.cryo:3372` | `ResolutionContext::new` | `ctx.source_file` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/sema/call_resolver.cryo:674` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/sema/call_resolver.cryo:1449` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/sema/call_resolver.cryo:1581` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/sema/call_resolver.cryo:1644` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/sema/call_resolver.cryo:1810` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/sema/call_resolver.cryo:4354` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/sema/call_resolver.cryo:4543` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/sema/call_resolver.cryo:4602` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/sema/call_resolver.cryo:4872` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/sema/call_resolver.cryo:4935` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/sema/call_resolver.cryo:5307` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/sema/call_resolver.cryo:5403` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/sema/call_resolver.cryo:5455` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/sema/call_resolver.cryo:5503` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/sema/lambda_synth.cryo:112` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/sema/method_binding.cryo:517` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/sema/method_binding.cryo:862` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/sema/method_binding.cryo:931` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/sema/method_binding.cryo:988` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/sema/method_binding.cryo:1104` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/sema/method_binding.cryo:1227` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/sema/method_binding.cryo:1249` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/sema/method_binding.cryo:1501` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/sema/method_binding.cryo:1696` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/sema/method_binding.cryo:1948` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/sema/sema.cryo:761` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/sema/sema.cryo:892` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/sema/sema.cryo:1582` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/sema/sema.cryo:2369` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/sema/sema.cryo:2621` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/sema/sema.cryo:3837` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/sema/symbolic_checker.cryo:130` | `ResolutionContext::new` | `""` | N | a constructor: the string is the SOURCE FILE the context resolves in |
| `compiler/passes/type_resolution.cryo:2330` | `TraitType::add_assoc_type` | `adecl.name` | N | a write: the trait's own associated-type name recorded on the trait type |
| `compiler/sema/call_resolver.cryo:4242` | `DeclarationIndex::lookup_method_return` | `spec_qn, scope.member_name` | S | the owner from `lookup_type_name(spec_ref)`, a `TypeRef`; the member a leaf inside it |
| `compiler/passes/type_resolution.cryo:445` | `DeclarationIndex::lookup_type` | `owner_key` | S | a type by a canonical name derived from a stamp |
| `compiler/types/resolver.cryo:1089` | `TypeArena::is_self_growing_instantiation` | ` ...` | S | the owner's qualified name off the impl target, compared with instantiation names in the arena |
| `compiler/types/resolver.cryo:1099` | `TypeArena::is_self_growing_instantiation` | ` ...` | S | the owner's qualified name off the impl target, compared with instantiation names in the arena |
| `compiler/sema/call_resolver.cryo:4219` | `TypeUtils::lookup_method_return` | `owner_sym, scope.member_name` | S | `(owner, member)`: the owner a canonical name off a `TypeRef` or stamp, the member a leaf inside it |
| `compiler/sema/call_resolver.cryo:4259` | `TypeUtils::lookup_method_return` | `owner_key, scope.member_name` | S | `(owner, member)`: the owner a canonical name off a `TypeRef` or stamp, the member a leaf inside it |
| `compiler/sema/call_resolver.cryo:5048` | `TypeUtils::lookup_method_return` | `di_name, scope.member_name` | S | `(owner, member)`: the owner a canonical name off a `TypeRef` or stamp, the member a leaf inside it |
| `compiler/sema/call_resolver.cryo:5543` | `TypeUtils::lookup_method_return` | `spec_qn, member_name` | S | `(owner, member)`: the owner a canonical name off a `TypeRef` or stamp, the member a leaf inside it |
| `compiler/sema/method_binding.cryo:201` | `TypeUtils::lookup_method_return` | `base_sym, method_name` | S | `(owner, member)`: the owner a canonical name off a `TypeRef` or stamp, the member a leaf inside it |
| `compiler/sema/method_binding.cryo:235` | `TypeUtils::lookup_method_return` | ` ...` | S | `(owner, member)`: the owner a canonical name off a `TypeRef` or stamp, the member a leaf inside it |
| `compiler/sema/method_binding.cryo:395` | `TypeUtils::lookup_method_return` | ` ...` | S | `(owner, member)`: the owner a canonical name off a `TypeRef` or stamp, the member a leaf inside it |
| `compiler/sema/method_binding.cryo:485` | `TypeUtils::lookup_method_return` | ` ...` | S | `(owner, member)`: the owner a canonical name off a `TypeRef` or stamp, the member a leaf inside it |
| `compiler/sema/method_binding.cryo:806` | `TypeUtils::lookup_method_return` | `blocks[k].qualified_trait_name, method_name` | S | `(owner, member)`: the owner a canonical name off a `TypeRef` or stamp, the member a leaf inside it |
| `compiler/sema/method_binding.cryo:823` | `TypeUtils::lookup_method_return` | `type_sym, method_name` | S | `(owner, member)`: the owner a canonical name off a `TypeRef` or stamp, the member a leaf inside it |
| `compiler/sema/method_binding.cryo:837` | `TypeUtils::lookup_method_return` | `parent_sym, method_name` | S | `(owner, member)`: the owner a canonical name off a `TypeRef` or stamp, the member a leaf inside it |
| `compiler/sema/sema.cryo:1271` | `TypeUtils::lookup_method_return` | `type_sym, this.intern.intern("next")` | S | the owner a canonical name off the iterated `TypeRef`; the member the for-in protocol's `next` |
| `compiler/sema/sema.cryo:1273` | `TypeUtils::lookup_method_return` | `type_sym, this.intern.intern("iter")` | S | the owner a canonical name off the iterated `TypeRef`; the member the for-in protocol's `iter` |
| `compiler/sema/call_resolver.cryo:2633` | `TypeUtils::lookup_type_exact` | `scope_sym` | S | a type by a canonical name derived from a stamp (`scope_owner_key`, `trait_identity`, `tr.identity()`) |
| `compiler/sema/call_resolver.cryo:3688` | `TypeUtils::lookup_type_exact` | `scope_sym` | S | a type by a canonical name derived from a stamp (`scope_owner_key`, `trait_identity`, `tr.identity()`) |
| `compiler/sema/call_resolver.cryo:3878` | `TypeUtils::lookup_type_exact` | `trait_sym` | S | a type by a canonical name derived from a stamp (`scope_owner_key`, `trait_identity`, `tr.identity()`) |
| `compiler/sema/call_resolver.cryo:3925` | `TypeUtils::lookup_type_exact` | `owner_sym` | S | a type by a canonical name derived from a stamp (`scope_owner_key`, `trait_identity`, `tr.identity()`) |
| `compiler/sema/call_resolver.cryo:5044` | `TypeUtils::lookup_type_exact` | `lookup_sym` | S | a type by a canonical name derived from a stamp (`scope_owner_key`, `trait_identity`, `tr.identity()`) |
| `compiler/sema/call_resolver.cryo:5730` | `TypeUtils::lookup_type_exact` | `lookup_sym` | S | a type by a canonical name derived from a stamp (`scope_owner_key`, `trait_identity`, `tr.identity()`) |
| `compiler/sema/diagnostics.cryo:282` | `TypeUtils::lookup_type_exact` | `type_sym` | S | a type by a canonical name derived from a stamp (`scope_owner_key`, `trait_identity`, `tr.identity()`) |
| `compiler/sema/method_binding.cryo:449` | `TypeUtils::lookup_type_exact` | `tr.identity()` | S | a type by a canonical name derived from a stamp (`scope_owner_key`, `trait_identity`, `tr.identity()`) |
| `compiler/sema/call_resolver.cryo:4216` | `TypeUtils::resolve_method_owner` | `lookup_sym, scope.member_name, null` | S | `(owner, member)`: the owner from `scope_owner_key` (a stamp), the member a leaf inside it |
<!-- residue-table:end -->

## Elsewhere: population names on types that hold nothing

Every dotted call to a population name whose receiver is placed on a type
that is NOT a holder, per (type, method). These are not sites - the type
holds no declaration - but they are pinned, because a holder the placement
rule misreads as another type lands here and nowhere else; this is the
lane gate's LOOKUP_LOCAL row, by name. A static call's owner is spelled and
cannot be misplaced, so statics on non-holders are not tallied. When a row
here moves, either a receiver was misplaced (place it) or a same-named
method on an unrelated type gained or lost a caller (regenerate, and say
which in the commit).

<!-- residue-elsewhere:begin -->
Elsewhere **18** - a population name on a type that holds nothing, per (type, method)

| receiver type and read | calls |
|---|---|
| `DropInserter::lookup_type` | 5 |
| `ModuleKeyTable::lookup` | 1 |
| `MoveChecker::lookup_type` | 4 |
| `Scope::is_ambiguous` | 2 |
| `ScopeManager::lookup_local` | 5 |
| `TypeMapperCache::lookup` | 1 |
<!-- residue-elsewhere:end -->
