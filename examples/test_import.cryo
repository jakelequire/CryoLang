namespace test::import;

import Types from <net/types>;

function test_import() -> i32 {
    const ip: Types::IpAddr = Types::IpAddr(u8(127), u8(0), u8(0), u8(1));
    const addr: Types::SocketAddr = Types::SocketAddr(ip, u16(8080));
    return 0;
}