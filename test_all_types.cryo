import <io/stdio>;

namespace Main;

function main() -> int {
    // Test all types with newlines
    std::IO::print_int(42);
    std::IO::print_float(3.14, 2);
    std::IO::print_bool(true);
    std::IO::print_bool(false);
    return 0;
}